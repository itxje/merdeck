# 20260919-0908-release-v0.16.0 Release v0.16.0

- **status**: completed
- **createdAt**: 2026-09-19 09:08
- **approvedAt**: 2026-09-19 09:08 UTC
- **relatedTask**: 20260919-0908-release-v0.16.0

## Context

The Markdown contents list and type scale are integrated into `main` as commit `b4783e6`. Local `bun run check`
passed with Bun 1.4.2 (289 backend tests, 568 web tests, lint, strict type checks, coverage and both production
builds), the browser suite passed 73 of 73 with 2 skipped by design, and native verification on `main` passed
as run 35431887434. The tag was owed from that request and never created.

## Proposal

1. Choose `v0.16.0`: the Markdown preview gains a contents list and a new type scale, so this is a minor bump.
2. Create the annotated tag on the already verified commit and push it alone.
3. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` are published, then verify the checksum.
4. Record the release outcome in the changelog and task documentation.

## Risks

- The owner's deployment follows the stable channel automatically, so the release reaches that instance within
  minutes of publication.

## Scope

Tag and release automation only; no source change.

## Alternatives

- A patch release would understate a visible layout and type change to every Markdown document.

## Outcome

Published `v0.16.0` from `b4783e6`. The tag workflow passed verify and publish on the first attempt, and the
published `merdeck.tar.gz` matches `SHA256SUMS`.

## Annotations

- Owner asked why v0.16.0 had not shipped on 2026-09-19.
