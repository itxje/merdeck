# 20260918-1701-release-v0.15.0 Release v0.15.0

- **status**: completed
- **createdAt**: 2026-09-18 17:01
- **approvedAt**: 2026-09-18 17:01 UTC
- **relatedTask**: 20260918-1701-release-v0.15.0

## Context

The agent context, approval and panel changes are integrated into `main` as commit `e13c194`. Local
`bun run check` passed with Bun 1.4.2 (288 backend tests, 564 web tests, lint, strict type checks, coverage and
both production builds), and the browser suite passed 73 of 73 with 2 skipped by design. Native verification on
`main` is running. The owner requested the release.

## Proposal

1. Await completion of native verification on `main` for commit `e13c194`.
2. Choose `v0.15.0`: the turn contract gains an optional field and the panel gains behavior, so this is a minor
   bump rather than a patch.
3. Create the annotated tag on the verified commit and push it alone.
4. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` are published, then verify the checksum.
5. Record the release outcome in the changelog and task documentation.

## Risks

- File work now proceeds without an operator confirmation, and the owner's deployment follows the stable
  channel automatically, so the release reaches that instance within minutes of publication.

## Scope

Tag and release automation only; no source change.

## Alternatives

- A patch release would understate a contract addition and a behavior change to approvals.

## Outcome

Published `v0.15.0` from `e13c194`. Native verification and the tag workflow both passed on the first attempt,
and the published `merdeck.tar.gz` matches `SHA256SUMS`.

## Annotations

- Owner asked for commit, push and release in the same request on 2026-09-18.
