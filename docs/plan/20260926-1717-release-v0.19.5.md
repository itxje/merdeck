# 20260926-1717-release-v0.19.5 Release v0.19.5

- **status**: completed
- **createdAt**: 2026-09-26 17:17
- **approvedAt**: 2026-09-26 17:17
- **relatedTask**: 20260926-1717-release-v0.19.5

## Context

The reviewed working tree contains the image-only 1 MiB data URL allowance and shared document-reader layout with focused unit/browser coverage. All functional stages passed; the prior aggregate refused evidence from uncommitted source. Main and the remote main currently agree. The latest published version is v0.19.4.

## Proposal

Commit the reviewed changes and tracking records. Run the standard clean-source local aggregate and push the same candidate to main for the existing native Linux x64/ext4 workflow. After both required acceptance results are observed, tag that exact immutable commit v0.19.5 and push the tag. Observe verification and publication, download both attachments, verify SHA256SUMS, embedded version and commit, then record the outcome.

## Scope

Current image and reader changes, task/plan/changelog records, main push and v0.19.5 publication. No dependency changes, new features or running-service deployment.

## Risks

Existing unrelated directory or authentication browser audit races may require an identical rerun; inspect actual failures first. Never move a release tag or weaken tests. Native acceptance must match the tagged bytes. Failed publication may leave a draft handled by the existing idempotent publisher.

## Alternatives

Publishing directly from the dirty tree or bypassing native acceptance would omit the repository's release guarantees and is excluded.

## Annotations

The user explicitly requested `release`, authorizing this delivery without another confirmation.

## Publication outcome

Published [v0.19.5](https://github.com/itxje/merdeck/releases/tag/v0.19.5) at 2026-09-26 17:41 UTC from immutable commit `51d9f1a5a48edd50b28ad48ca0422407b423492a`. The [tag workflow](https://github.com/itxje/merdeck/actions/runs/36259187891) passed verification and publication on attempt 1.

Downloaded `merdeck.tar.gz` (1,385,532 bytes) and `SHA256SUMS` from the published release. `sha256sum --check` passes; archive SHA-256 is `517e032c52554b4216e3d144dcadee327bd8cc1eedcfb3a91f5c6b629a88bc92`. The archive has 97 entries. The extracted bundle reports `Merdeck 0.19.5`; `--build-info` confirms tag `v0.19.5`, the exact release commit, target `bundle` and Bun `1.4.2`.

Release notes describe the embedded image correction and shared HTML/Markdown reader, retain the publisher provenance marker and state the tagged runtime support contract. Evidence: `tmp/release-v0195-download/`, `tmp/release-v0195-published.json` and `tmp/release-v0195-notes.md`. No running service was updated.
