# 20260919-1735-release-v0.16.1 Release v0.16.1

- **status**: completed
- **createdAt**: 2026-09-19 17:35
- **approvedAt**: 2026-09-19 17:35 UTC
- **relatedTask**: 20260919-1735-release-v0.16.1

## Context

The Markdown contents-list mobile-layout fix is integrated into `main` as commit `b1df294`. Local web lint,
`tsc --noEmit` and the `document-view` test suite passed, and the fix was verified directly against the real
markup and CSS at 1440px and 500px viewports. Native verification on `main` for this commit is in progress as
[run 35458215193](https://github.com/itxje/merdeck/actions/runs/35458215193). The owner asked to release.

## Proposal

1. Choose `v0.16.1`: a single internal CSS bug fix, no contract or interface change, so a patch bump.
2. Wait for native verification on `main` to pass for `b1df294`.
3. Create the annotated tag on that verified commit and push it alone.
4. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` are published, then verify the checksum.
5. Record the release outcome in the changelog and task documentation.

## Risks

- The owner's deployment follows the stable channel automatically, so the release reaches that instance within
  minutes of publication.

## Scope

Tag and release automation only; no source change.

## Alternatives

- Holding the fix for a later release would leave the Markdown contents list unusable on narrow/mobile
  viewports on the deployed instance.

## Outcome

Verification passed on `main` for `b1df294`, the tag was published with `merdeck.tar.gz` and `SHA256SUMS` on
2026-09-19 at 17:53:18Z, and the archive's checksum matches the published sums.

## Annotations

- Owner asked to release on 2026-09-19.
