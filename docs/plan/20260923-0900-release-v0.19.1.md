# 20260923-0900-release-v0.19.1 Release v0.19.1

- **status**: completed
- **createdAt**: 2026-09-23 09:00
- **approvedAt**: 2026-09-23 09:00 UTC
- **relatedTask**: 20260923-0900-release-v0.19.1

## Context

The Claude Code permission channel fix is integrated into `main` as commit `2a7eb87`. Local `bun run check`
passed with Bun 1.4.2 (292 backend tests, 579 web tests, lint, strict type checks, coverage and both
production builds), the browser suite passed 100 with 7 skipped by design, and native verification on `main`
passed as run 35839330165.

## Proposal

1. Choose `v0.19.1`: launch arguments and the adapter's permission decision change; no contract or interface
   change.
2. Create the annotated tag on the verified commit and push it alone.
3. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` are published, then verify the checksum.
4. Record the release outcome in the changelog and task documentation.

## Risks

- If a provider build stopped routing decisions to the host, writes would be refused rather than allowed.
  Both installed builds were measured to route them.

## Scope

Tag and release automation only; no source change.

## Alternatives

- Holding the fix would leave the adapter's refusals unreachable on the deployed instance.

## Annotations

- Delivered under the standing arrangement to release a completed fix without asking.

## Outcome

Tag `v0.19.1` was created on `2a7eb87` and pushed alone. The tag workflow passed verification and publication
on the first attempt, publishing `merdeck.tar.gz` (1383587 bytes) and `SHA256SUMS`; `sha256sum -c` reports OK
on the downloaded archive, and its bundle launches the provider with the stdio prompt handler. No source change
was made under this plan.
