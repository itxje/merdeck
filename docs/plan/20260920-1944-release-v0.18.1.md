# 20260920-1944-release-v0.18.1 Release v0.18.1

- **status**: completed
- **createdAt**: 2026-09-20 19:44
- **approvedAt**: 2026-09-20 19:44 UTC
- **relatedTask**: 20260920-1944-release-v0.18.1

## Context

The owner found, on the running instance, that a file name and its extension sat at opposite ends of a wide
row. The cause is on `main` as commit `2b48c86`: wrapping split a row name into a stem and an extension, and
the stem was allowed to grow into the row's free width. Native verification on `main` passed for that commit.
The owner asked for releases to follow a fix without being asked for.

## Proposal

1. Choose `v0.18.1`: one correction to presentation shipped in v0.17.0, no new behaviour and no contract
   change, so a patch bump.
2. Create the annotated tag on the verified commit and push it alone.
3. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` are published, then verify the checksum
   against the downloaded archive rather than trusting the workflow's own result.
4. Record the release outcome in the changelog and task documentation.

## Risks

- The defect is visible in every listing wide enough to show it, so reaching the instance quickly is the
  point rather than a hazard.

## Scope

Tag and release automation only; no source change.

## Alternatives

- Waiting for more work to accumulate would leave every wide listing showing each name in two pieces.

## Outcome

Verification passed on `main` for `2b48c86`, the tag workflow published `merdeck.tar.gz` and `SHA256SUMS` on
2026-09-20 at 19:55:29Z, and `sha256sum -c` reports OK on the downloaded archive.

## Annotations

- Owner asked for the release to follow the fix on its own on 2026-09-20.
