# 20260926-1953-release-v0.19.6 Release v0.19.6

- **status**: implementing
- **createdAt**: 2026-09-26 19:53
- **approvedAt**: 2026-09-26 19:53
- **relatedTask**: 20260926-1953-release-v0.19.6

## Context

The installed v0.19.5 bundle predates the mobile contents correction. Reviewed changes keep the drawer inside the viewport, remove the Markdown mode toolbar, align prose/tables/diagrams within a shared 80rem measure and contain table scrolling. Local aggregate acceptance passed on clean code; subsequent changes are documentation only.

## Proposal

Record and commit the release candidate, push main, and observe exact-commit Linux x64/ext4 native acceptance. Tag that immutable commit v0.19.6 only after success. Observe the standard tag verification and publisher, then download the archive and checksum, verify the hash, version, tag and commit, and record the result.

## Scope and risks

Main push, immutable tag and GitHub release publication with existing checks. No code or dependency changes and no running-service restart. Inspect any workflow failure before rerunning unchanged bytes; never weaken checks or move the tag. Physical-iPhone confirmation remains separate from the verified browser evidence.

## Verification

Reuse the recent clean-source local aggregate for unchanged implementation bytes; verify the code diff since `856be92` is empty. Run the normal exact-commit native workflow before tagging and allow the tag workflow to repeat its mandatory gate. Validate actual downloaded assets and preserve release provenance in the notes.

## Authorization

The user's explicit `release` request authorizes this publication without another confirmation.
