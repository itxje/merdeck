# 20260919-0108-release-v0.15.1 Release v0.15.1

- **status**: completed
- **createdAt**: 2026-09-19 01:08
- **approvedAt**: 2026-09-19 01:08 UTC
- **relatedTask**: 20260919-0108-release-v0.15.1

## Context

The streamed-turn budget fix is integrated into `main` as commit `493b53b`. Local `bun run check` passed with
Bun 1.4.2 (289 backend tests, 564 web tests, lint, strict type checks, coverage and both production builds),
and native verification on `main` passed as run 35376358949. The owner requested the release.

## Proposal

1. Choose `v0.15.1`: one internal accounting fix, no contract or interface change.
2. Create the annotated tag on the verified commit and push it alone.
3. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` are published, then verify the checksum.
4. Record the release outcome in the changelog and task documentation.

## Risks

- The owner's deployment follows the stable channel automatically, so the release reaches that instance within
  minutes of publication.

## Scope

Tag and release automation only; no source change.

## Alternatives

- Holding the fix for a later release would leave ordinary turns failing on the deployed instance.

## Outcome

Published `v0.15.1` from `493b53b`. The tag workflow passed verify and publish on the first attempt, and the
published `merdeck.tar.gz` matches `SHA256SUMS`.

## Annotations

- Owner approved the release on 2026-09-19.
