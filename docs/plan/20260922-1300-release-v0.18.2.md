# 20260922-1300-release-v0.18.2 Release v0.18.2

- **status**: completed
- **createdAt**: 2026-09-22 13:00
- **approvedAt**: 2026-09-22 13:00 UTC
- **relatedTask**: 20260922-1300-release-v0.18.2

## Context

The contents-list layout fix is integrated into `main` as commit `fb2ce35`. Local `bun run check` passed with
Bun 1.4.2 (289 backend tests, 572 web tests, lint, strict type checks, coverage and both production builds),
the browser suite passed 105 of 105 with 2 skipped by design, and native verification on `main` passed as
run 35729050682.

## Proposal

1. Choose `v0.18.2`: presentation only, no contract or behaviour change.
2. Create the annotated tag on the verified commit and push it alone.
3. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` are published, then verify the checksum.
4. Record the release outcome in the changelog and task documentation.

## Risks

- The deployment follows the stable channel automatically, so the release reaches that instance within minutes
  of publication.

## Scope

Tag and release automation only; no source change.

## Alternatives

- Holding the fix for a later release would leave the contents list stranded mid-pane on a wide preview.

## Outcome

Published `v0.18.2` from `fb2ce35`. The tag workflow needed one rerun after a browser console error that no
other run of the same commit reproduced; the rerun passed verify and publish, and the published
`merdeck.tar.gz` matches `SHA256SUMS`.

## Annotations

- Delivered under the standing arrangement to release a user-visible fix without asking.
