# 20260922-2120-release-v0.18.4 Release v0.18.4

- **status**: implementing
- **createdAt**: 2026-09-22 21:20
- **approvedAt**: 2026-09-22 21:20 UTC
- **relatedTask**: 20260922-2120-release-v0.18.4

## Context

The assistant write fix is integrated into `main` as commit `57a859e`. Local `bun run check` passed with Bun
1.4.2 (289 backend tests, 577 web tests, lint, strict type checks, coverage and both production builds), the
browser suite passed 105 of 105 with 2 skipped by design, and native verification on `main` passed as
run 35784730256.

## Proposal

1. Choose `v0.18.4`: one launch-argument change, no contract or interface change.
2. Create the annotated tag on the verified commit and push it alone.
3. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` are published, then verify the checksum.
4. Record the release outcome in the changelog and task documentation.
5. Confirm the deployed service can change a file once it follows the release.

## Risks

- The change widens what the provider pre-approves, so the release is recorded with the measurement that the
  project boundary is enforced by the provider's own working-directory confinement.
- The deployment follows the stable channel automatically, so the release reaches that instance within
  minutes.

## Scope

Tag and release automation only; no source change.

## Alternatives

- Holding the fix would leave the panel unable to perform the one action it exists for.

## Annotations

- Delivered under the standing arrangement to release a user-visible fix without asking.
