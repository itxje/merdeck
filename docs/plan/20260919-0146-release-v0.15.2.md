# 20260919-0146-release-v0.15.2 Release v0.15.2

- **status**: completed
- **createdAt**: 2026-09-19 01:46
- **approvedAt**: 2026-09-19 01:46 UTC
- **relatedTask**: 20260919-0146-release-v0.15.2

## Context

The engine/model selection persistence fix and the wider HTML reading measure are integrated into `main` as
commit `4eb7898`. Local web lint, `tsc --noEmit`, full web `test:coverage` (352 tests) and both production
builds passed with Bun 1.4.2, and native verification on `main` passed as
[run 35413195618](https://github.com/itxje/merdeck/actions/runs/35413195618). The owner asked to commit, push
and release.

## Proposal

1. Choose `v0.15.2`: two small internal fixes, no contract or interface change.
2. Create the annotated tag on the verified commit and push it alone.
3. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` are published, then verify the checksum.
4. Record the release outcome in the changelog and task documentation.

## Risks

- The owner's deployment follows the stable channel automatically, so the release reaches that instance within
  minutes of publication.

## Scope

Tag and release automation only; no source change.

## Alternatives

- Holding the fixes for a later release would leave the AI file editor forgetting its selection and the HTML
  preview narrower than requested on the deployed instance.

## Outcome

Published `v0.15.2` from `4eb7898`. The tag workflow passed verify and publish, and the published
`merdeck.tar.gz` matches `SHA256SUMS`.

## Annotations

- Owner asked to commit, push and release on 2026-09-19.
