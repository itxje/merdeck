# 20260916-2030-release-v0.14.1 Release v0.14.1

- **status**: approved
- **createdAt**: 2026-09-16 20:30
- **approvedAt**: 2026-09-16 20:30 UTC
- **relatedTask**: 20260916-2030-release-v0.14.1

## Context

The HTML preview improvements (removing notice banner, rich media and style support, full screen width utilization) and Google Antigravity engine fixes (label update, interactive CLI spinner stripping, `lode.toml` configuration) have been integrated into `main` and verified natively in hosted CI run `35145625156` on Linux x64/ext4. The user requested release.

## Proposal

1. Create annotated tag `v0.14.1` on the verified commit.
2. Push tag `v0.14.1` to GitHub.
3. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` release assets are published and verified.
4. Record release outcome in changelog and task documentation.
