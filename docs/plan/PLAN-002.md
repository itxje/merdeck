# PLAN-002 Verify CI and single-executable releases

- **status**: completed
- **createdAt**: 2026-09-07
- **approvedAt**: 2026-09-07 (original explicit scope authorization; day precision)
- **relatedTask**: CI-001

## Context and investigation

CI-001 was claimed and re-read before substantive investigation. The integrated application has separate frozen root/web locks, Bun 1.4.2 and Node 24.20.0 pins, real source coverage, file/procfs/storage checks and nineteen browser cases. `build` creates Bun code plus Vite output; startup snapshots trusted assets into a map, and requests never resolve asset paths. No release automation or executable exists at this baseline. The prototype remains needs-review with designSystems=[]; no visual change is proposed.

The local host is Linux arm64. Allowed external scratch is overlayfs; the checkout is a distinct unsupported host-shared filesystem. Neither is native ext4. No x64 emulator or file-access tracer is installed. Project-local verification tools can be provisioned; cross-compilation is not execution evidence. PLAN-005 requires exactly twenty unchanged native raw probes and actual policy admission. Current production policy refuses ext4: the first matching hosted check must fail visibly after preserving evidence. No admission change is authorized here.

Official sources checked on 2026-09-07: Bun executable documentation supports explicit linux-arm64/linux-x64 targets, build constants, asset embedding and disabling automatic runtime config discovery. GitHub workflow_dispatch requires the workflow on the default branch; initial verification also needs a narrow branch push trigger. The release API supports drafts, generated notes and assets. Current action versions/full tag commit resolutions and actionlint v1.7.12 will be recorded in the CI/release decision before use. Application dependency upgrades are unnecessary.

## Proposal

1. Preserve React/Vite and Hono. Build Vite first, enumerate every output through the trusted-asset loader, generate embedded assets/version inputs under ignored build scratch and compile an explicit target. Inject the map at startup, retaining exact API/health/static/header behavior. Never rewrite tracked source or embed the configured project/token. Add credential-free version output; executable configuration remains explicit environment input.
2. Produce raw `diagramdock-<validated-version>-linux-<architecture>` plus SHA256SUMS; no archive is necessary. Validate SemVer/tag, target and commit metadata; reject mismatches/unexpected inventory/partial output. Use only explicit nonpublishing fixture versions in checks. No initial release version or tag is selected.
3. Implement TypeScript setup, workflow validation, compilation and release checks. `check:ci` runs source `check`, explicit file/storage gates, workflow lint, release tests and compile once each. Binary smoke delegates exactly once to existing `test:e2e`, exercising the complete production browser contract without a duplicate source-browser run.
4. Copy only the executable into exclusive outside-checkout scratch. Start through owned tmux with empty runtime PATH and private credentials; trace filesystem/process calls, compare every HTTP asset with its inventory and exercise lazy Mermaid plus authenticated browser browse/edit/save/conflicts/Markdown preservation. Verify no extraction/runtime/source dependency, service shutdown and scoped cleanup. Report local arm64 separately from pending x64/native acceptance.
5. Add read-only PR/main checks and nonpublishing `verify/native-readiness` push/manual checks. Hosted Linux x64 identifies actual native and refusal storage, records run/commit/runner metadata and invokes PLAN-005. Any identity/raw/admission failure remains failure with redacted artifacts. After justified policy correction the same path must continue through source and native binary-browser gates. No mount manipulation or fabricated identity.
6. Tags run the complete matching-runner native gate before a separate publisher receives checked artifacts. Verify immutable action pins; disable persisted checkout credentials; default contents:read; bounded retention/timeouts; keys include both locks and runtime pins. Only publisher gets contents:write, restores no writable cache and stages a matching draft. Verify/upload complete assets before publishing. Reruns reuse identical matching output or fail safely, never delete unrelated releases/tags/assets. Test with nonpublishing mock transport.

## Authorization and scope

The user explicitly authorized the MVP, git@github.com:itxje/diagramdock.git, CI/version-tag automation, single-executable clarification and direct external editing on 2026-09-07. This proposal uses that existing authorization after investigation; it invents no later approval. The earlier runtime-required backend/frontend archive is superseded. Final main integration remains a separate approval boundary. No push, tag, release or remote execution occurs here.

Scope: workflows/TypeScript automation, focused CI/release tests, necessary scripts, embedded-asset/build-info startup wiring and focused documentation/tracking. File admission, auth/editor protocols and prototype stay unchanged. Dependency: TEST-001 -> NATIVE-001 preparation -> CI-001 -> REVIEW-001 -> DOC-001; final native acceptance is independent.

## Risks and alternatives

Embedding must include lazy chunks/fonts and preserve MIME/CSP. Compile failure/interruption must remove only generated inputs/partial output. Tracing might be unavailable; report the concrete gap rather than claim independence. Cross-compilation cannot establish x64 execution. Runner OS does not identify its filesystem; refused/missing native storage blocks publication. Package checks cannot erase practical final-comparison/rename or OS-actor limits; the diagnostic remains applicationSafetyPassed=false.

Raw executable plus checksum avoids a redundant archive. Generated inputs avoid source-stub swaps. TypeScript API publication allows tested draft/rerun logic without extra credentials or registries. Source development retains pinned tooling; downloaded executables require no installed JavaScript runtime or frontend directory.

## Verification

Final local tmux command: `bun install --frozen-lockfile && bun install --cwd web --frozen-lockfile && bun run check:ci && git diff --check`, with explicit observed fixture parents/types, project-local Chromium and pinned runtime PATH. Record stage counts, actionlint version/pins, compile success/failure/interruption cleanup, binary host/target/version/checksum, complete assets, actual traced accesses/processes, browser assertions/screenshots/URLs and shutdown. Apply shared and TypeScript backend/frontend review policies. Remote/native/x64 remain pending until observed; overall PLAN-001 remains implementing.


## Verification and completion

Completed the bounded automation implementation and local preparation on 2026-09-07 22:05. [CI-001](../task/CI-001.md) records the clean implementation commit, exact mandatory command, runtime/storage/asset/version/checksum/browser evidence, original failed trace audit and focused correction, independent cleanup and scoped review. The final full command exited 0: workflow lint, 85 focused file tests, 6 storage preparation tests, 1 procfs test, 122 backend tests, 43 frontend tests, 11 release/CI tests, full build, ARM64 executable, all 93 HTTP asset matches, 19 real binary browser cases and four lazy-rendering families. Linux x64 cross-compilation and ELF/checksum checks passed separately; no x64 execution occurred.

NativeAcceptance remains pending and the first real ext4 runner must still collect raw evidence then fail current admission. Actual remote CI/release and x64/native browser stages remain unrun. This plan completes implementation preparation, not release readiness or the overall MVP. The concrete nonpublishing candidate branch and maintainer commands are in README; tag publication cannot run before actual matching-runner native success. PLAN-001 remains implementing, downstream review/delivery remain pending, the prototype remains needs-review and no main merge/push/tag/release was performed.

## Bounded trace correction: investigation and proposal

CI-001 was reclaimed before investigation under the existing authorization. Prior successful local results remain historical. Independent integration subsequently failed after nineteen browser passes, all 93 assets and four lazy families: the audit treated an unfinished openat as a successful write without its returned descriptor. The retained trace shows thread 55035 starting a scoped atomic temporary open, thread 55193 starting statx, and thread 55035 resuming with fd 20 under the configured synthetic root. The integration result is failed with cleanup=true; a separate shell wrapper's success marker is invalid because cleanup masked the failed AND-list. Neither the trace nor that failure record will be modified.

Repository preservation checks: retained delivery `28c38b95f05f692b48637fc59dbb0b4de1fc9616` is clean before reclaim; foundation `7c24ec8c2b8187679193ee571e8452f3930a13a9` remains its ancestor. Current integration `c086fe955c449426b06513840a3fd6fe6c36f3d0` has a byte-identical tracked tree to that foundation. The intentional integration rollback must not be merged over retained implementation. No new feature synchronization is necessary.

Proposal: normalize unfinished/resumed records by process/thread identity and exact syscall before the existing audit. Preserve original arguments, return values and descriptor targets, including negative-return opens. Reject missing, duplicate, malformed or mismatched relevant records rather than ignore them. Retain source/runtime/extraction restrictions and the returned-fd rule for AT_FDCWD annotations. Count paired calls separately for reproducible evidence. The [official strace manual](https://github.com/strace/strace/blob/master/doc/strace.1.in) and the retained real trace define the relevant interleaved format.

Scope is only scripts/release/trace.ts, tests/release/trace.test.ts and task/plan correction notes. No dependency, workflow, production, threshold or admission changes. Dropping unfinished records or allowing missing write targets would hide unsafe writes and is rejected. The primary risk is pairing concurrent calls incorrectly; identity/syscall matching and fail-closed completeness checks address it.

Verification: test the observed interleaving, concurrent identities/syscalls, negative returns, split source/extra-runtime/extraction access, malformed or incomplete pairs and existing single-line controls. Replay both full retained integration traces read-only with their original executable/root/checkout identities, recording paired-call and successful-write counts. Then run focused strict types and test:release, followed by one complete frozen-install/check:ci/diff gate in the owned tmux session with fresh explicit supported/refused roots. Capture the command status before independent cleanup. Native/ext4, actual x64 and remote acceptance remain pending. The existing authorization permits this focused correction without a new approval event.

Correction completed at implementation `74154938a579d996df00c0f0692290fdb3a8b2c7`. Both original traces replay successfully and remain byte-identical: supported 392 pairs / 9 writes, refused 7 pairs / 0 writes. Focused strict checks and 18 CI/release tests passed. One complete mandatory local command at the clean correction commit exited 0, including nineteen actual binary browser cases, all 93 assets and four lazy families. New traces passed with 342 supported pairs / 9 writes and 5 refused pairs / 0 writes. Independent shutdown/URL/fixture/runtime/credential/compile-input cleanup passed. CI-001 records exact hashes, paths, counts, the retained original integration failure, the first focused device-annotation validation failure and the scoped passing review. This completes only the bounded correction; native/x64/remote delivery acceptance remains pending and the earlier independent integration run remains failed.
