# 20260923-0820-release-v0.19.0 Release v0.19.0

- **status**: completed
- **createdAt**: 2026-09-23 08:20
- **approvedAt**: 2026-09-23 08:20 UTC
- **relatedTask**: 20260923-0820-release-v0.19.0

## Context

Conversation continuity and the New control are integrated into `main` as commit `d3f3a19`. Local
`bun run check` passed with Bun 1.4.2 (292 backend tests, 579 web tests, lint, strict type checks, coverage
and both production builds), the browser suite passed 100 with 7 skipped by design, and native verification
on `main` passed as run 35834816048.

## Proposal

1. Choose `v0.19.0`: the change adds `POST /agents/conversations/:id/close`, an API addition.
2. Create the annotated tag on the verified commit and push it alone.
3. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` are published, then verify the checksum.
4. Record the release outcome in the changelog and task documentation.

## Risks

- The deployment follows the stable channel automatically, so the release reaches that instance within
  minutes; a live conversation there ends when the service restarts onto the new version.

## Scope

Tag and release automation only; no source change.

## Alternatives

- Holding the change would leave a failed turn discarding the exchange behind it.

## Annotations

- Delivered under the standing arrangement to release a user-visible change without asking.

## Outcome

Tag `v0.19.0` was created on `d3f3a19` and pushed alone. The tag workflow passed verification and publication
on the first attempt, publishing `merdeck.tar.gz` (1383496 bytes) and `SHA256SUMS`; `sha256sum -c` reports OK
on the downloaded archive, and its bundle carries version `0.19.0`. No source change was made under this plan.
