# 20260926-1135-release-v0.19.3 Release v0.19.3

- **status**: completed
- **createdAt**: 2026-09-26 11:35
- **approvedAt**: 2026-09-26 11:34 UTC
- **relatedTask**: 20260926-1135-release-v0.19.3

## Context

The phone Files drawer repair is committed on local main at `76023ee`, two commits ahead of origin/main. The clean source commit passed the local ARM64/overlay `check:ci`, including 579 frontend tests, two 92-case browser runs, bundle smoke checks and evidence export. The latest published release is v0.19.2. Native Linux x64/ext4 acceptance is required for the new exact candidate before tagging.

## Proposal

1. Record release tracking and push the resulting main candidate.
2. Require the GitHub main workflow to pass native Linux x64/ext4 source and executable acceptance on that exact commit.
3. Create and push annotated tag v0.19.3 on the accepted commit; require the tag workflow to verify and publish.
4. Download `merdeck.tar.gz` and `SHA256SUMS`, check the archive checksum and embedded version/commit, and record the result.

## Risks

- Native verification or publication may fail. Keep the failing evidence and fix or rerun the identical candidate; do not move a published tag.
- The release publishes a bundle but does not update an independently running service. Native iPhone gesture validation requires a deployment with this build.

## Scope

Release records, push, native verification, tag publication and artifact verification. No application source changes.

## Alternatives

- Holding the release leaves the phone scrolling repair unavailable in published bundles.

## Annotations

- 2026-09-26 11:34: The owner explicitly requested release, authorizing the publication steps and satisfying the PMA proposal gate.

## Outcome

`main` was pushed at `16d580f2bb05be1de94b8b070acaab5860e38dc3`. Its [Linux x64/ext4 native verification](https://github.com/itxje/merdeck/actions/runs/36239299434) passed, including the source, executable and browser checks. Annotated tag `v0.19.3` resolves to the same commit. The [tag workflow](https://github.com/itxje/merdeck/actions/runs/36239940780) passed both verification and publication on its first attempt. The published [v0.19.3 release](https://github.com/itxje/merdeck/releases/tag/v0.19.3) contains `merdeck.tar.gz` and `SHA256SUMS`. The downloaded archive passed `sha256sum --check`, contains 97 entries, embeds the tagged commit and reports `Merdeck 0.19.3` from its extracted entry point. No running service was updated.
