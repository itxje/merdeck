# 20260922-1532-release-v0.18.3 Release v0.18.3

- **status**: completed
- **createdAt**: 2026-09-22 15:32
- **approvedAt**: 2026-09-22 15:32 UTC
- **relatedTask**: 20260922-1532-release-v0.18.3

## Context

The image-boundary fix is integrated into `main` as commit `c20ee7d`. Local `bun run check` passed with Bun
1.4.2 (289 backend tests, 577 web tests, lint, strict type checks, coverage and both production builds), the
browser suite passed 105 of 105 with 2 skipped by design, and native verification on `main` passed as
run 35746561831.

## Proposal

1. Choose `v0.18.3`: one narrowed refusal rule, no contract or interface change.
2. Create the annotated tag on the verified commit and push it alone.
3. Monitor the tag workflow until `merdeck.tar.gz` and `SHA256SUMS` are published, then verify the checksum.
4. Record the release outcome in the changelog and task documentation.

## Risks

- The rule guards the preview, so the narrowing is recorded with the reasoning that both real image forms stay
  refused, the destination form twice over.
- The deployment follows the stable channel automatically, so the release reaches that instance within minutes.

## Scope

Tag and release automation only; no source change.

## Alternatives

- Holding the fix would leave any label carrying an attribute able to take its whole diagram down.

## Annotations

- Delivered under the standing arrangement to release a user-visible fix without asking.

## Outcome

Tag `v0.18.3` was created on `c20ee7d` and pushed alone. The tag workflow passed verification and publication
on the first attempt, publishing `merdeck.tar.gz` (1383351 bytes) and `SHA256SUMS`; `sha256sum -c` reports OK
on the downloaded archive. The release is recorded in the changelog and the task documentation. No source
change was made under this plan.
