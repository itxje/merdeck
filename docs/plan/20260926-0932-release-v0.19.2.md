# 20260926-0932-release-v0.19.2 Release v0.19.2

- **status**: implementing
- **createdAt**: 2026-09-26 09:32
- **approvedAt**: 2026-09-26 09:32 UTC
- **relatedTask**: 20260926-0932-release-v0.19.2

## Context

The repair is committed on local `main` at `9ab662f75fee02ea97cfdf86ef0a34be783ebed8`, one commit ahead of `origin/main`. The local ARM64/overlay `check:ci` passed on that commit. The latest published release is `v0.19.1`; `v0.19.2` passes the repository version validator. The release workflow verifies a pushed `main` candidate on Linux x64/ext4, then verifies an annotated version tag again before publishing the archive and checksum.

## Proposal

1. Record this release task and push the candidate on `main`.
2. Wait for native Linux x64/ext4 acceptance on the exact pushed commit. Do not create a tag on a failed candidate.
3. Create and push annotated tag `v0.19.2` on the accepted commit. Let the tag workflow verify and publish.
4. Download `merdeck.tar.gz` and `SHA256SUMS`, verify the checksum and bundle identity, then record the outcome.

## Risks

- Hosted acceptance or publication may fail. Retain the exact failing evidence and repair the candidate or rerun an identical resumable publication; do not move a published tag.
- The release publishes source and assets but does not update a separately running deployment.

## Scope

Release metadata, push, native verification, tag publication and artifact verification. No application source changes.

## Alternatives

- Holding the release leaves the two phone-facing repairs unavailable to users of published bundles.

## Annotations

- 2026-09-26 09:32: Owner explicitly requested push and release, satisfying the publication authorization and PMA proposal gate.
