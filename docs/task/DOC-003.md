# DOC-003 Keep hosted instance hostnames and addresses out of the repository

- **status**: completed
- **priority**: P1
- **owner**: Documentation maintainer
- **createdAt**: 2026-09-11 10:18

## Description

After `main` was pushed to the public repository, the project owner asked on 2026-09-11 to remove the hosted instance's hostname from the repository and to reset the commit history. The hosted instance runs with open access, and its hostname appeared in documentation throughout the history. Acceptance: no tracked file names the real hostnames or ingress addresses, and the published history contains no earlier commits.

## ActiveForm

Removing hosted instance hostnames and addresses from the repository.

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

- Investigation (2026-09-11): the current hostname (49 mentions), the earlier hostname (25) and their shared domain (4) appeared only in Markdown documentation, never in code or configuration, and one earlier commit message also named the domain. Private ingress addresses appeared in seven documents.
- Implementation (2026-09-11): replaced them with reserved names (`merdeck.example.test`, `diag.example.test`, `*.example.test`, `example.test`) and documentation addresses (`192.0.2.1`, `192.0.2.5:33003`, `192.0.2.7`), rephrased the two sentences that described the address range, and noted the substitution at the top of the deployment record and in the changelog. Five older task notes also named an internal ignored helper directory and now say `tmp/`. The owner-private launcher and ignored helper files keep the real hostname, so the service still accepts its origin. `main` is published as a single new root commit, and the previous history is kept only in a local tag.
- Verification (2026-09-11): no tracked or new file names the real hostnames, the private addresses or the internal helper directory, and a whole-tree scan for internal coordination, tool and vendor names matched only minified third-party code inside the prototype bundle. Root ESLint and `git diff --check` passed.
