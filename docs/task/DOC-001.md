# DOC-001 Deliver reproducible instructions and evidence

- **status**: completed
- **priority**: P1
- **owner**: Documentation maintainer
- **createdAt**: 2026-09-07 17:42

## Description

Replace bootstrap-only README status with verified setup/run and quality instructions for the complete MVP. Document explicit project-root configuration, supported formats/fence behavior, sample project, save conflicts, external edits, auth, reverse proxy, production delivery, limitations and recovery. Summarize real check/preview evidence in canonical task and plan records.

## ActiveForm

Delivering reproducible instructions and evidence

## Dependencies

- **blocked by**: REVIEW-001
- **blocks**: (none)

## Notes

### Delivery preparation: investigation and proposal (2026-09-07)

- Claimed and re-read the index/detail as Documentation maintainer before investigation. Preparation baseline: `4ca471274d3ffa76469f8b2e87d4abd7055aa764`, including the accepted finite review `668db53`, ext4 candidate preparation and the save-observation correction. The working tree was clean before the claim. The original 2026-09-07 authorizations cover the MVP, exact repository origin, CI/version-tag automation, one embedded executable and direct external editing; no later approval is invented.
- Investigation: current source serves the authenticated explorer/editor/strict preview and same-origin SPA, and automation embeds all frontend assets. AGENTS, architecture and README retain contradictory starter/root-404/incomplete-gate/embedding-pending prose. The prototype metadata is still `needs-review`, `designSystems: []`. The first hosted run `34166805672` failed old ext4 admission at `c1078e302cfe43eeaa8fc5bf9286a09273a74ee5` after twenty raw passes. The corrected candidate's full native/x64 evidence and final review remain pending.
- Stage split: the accepted finite review supplies the concrete P3 documentation input, so bounded preparation may proceed while REVIEW-001 remains in progress. Final DOC completion still depends on corrected review and actual native source/executable acceptance. Keep DOC in progress and PLAN-001 implementing; leave all other owners/statuses unchanged.
- Proposal: reconcile current public documentation against source, preserve dated failure/coverage/commit provenance, replace internal absolute scratch identifiers only with truthful neutral descriptions or existing relative references, and document exact source setup, explicit root/token/origins, tmux/nsl development, independent production startup, single-executable distribution and practical save/storage limits. Do not alter runtime, tests, dependencies, locks, samples or design assets.
- Verification: provision only ignored project-local pinned tools; run both frozen installs and `bun run build`; exercise documented production startup on an explicitly identified disposable overlay sample and a separate own shared refusal fixture. Run the existing HTTP smoke and focused real desktop/narrow browser checks, inspect screenshots and any served prototype, verify links/configuration/shell examples and source/lock/design preservation, then `git diff --check`. Do not repeat the unchanged full `check:ci` or present historical counts as this run. Stop owned services before fixture/credential cleanup unless a clearly identified private preview is retained.
- Risks: documentation can overstate native applicability, remote reachability or binary proof. Keep local source checks distinct from historical compiled ARM64 checks and pending corrected hosted ext4/x64 acceptance. Retain the final comparison/rename loss window, local OS actor limits and `applicationSafetyPassed=false` diagnostic. Final main integration and any real release remain separate concrete approval/verification boundaries.

- Native delivery evidence follows PLAN-005. Report local overlay checks separately from native source, hosted run and standalone executable browser results. Preserve nativeAcceptance=pending until the actual deployed native-root flow passes; include exact commit/run URL/filesystem identity, cleanup and remaining practical limitations. This note does not change this task's owner or status.

- Claim before investigation, re-read index/detail, and persist investigation/proposal here. Reconcile the [plan](../plan/PLAN-001.md), [architecture](../architecture.md), README, example configuration and implementation before claiming delivery.
- Execute documented setup with the pinned runtime and frozen root/frontend lockfiles, build both packages and start production against an explicit sample root without nsl. Use tmux and project-local scratch; never alter operator project files for smoke checks.
- Final aggregate gate: `bun install --frozen-lockfile && bun install --cwd web --frozen-lockfile && bun run check:ci && git diff --check`, with its documented prerequisites and separate actual native acceptance. This documentation-only preparation uses the focused setup/build/HTTP/browser subset above; it does not rerun the unchanged aggregate. Inspect all relative docs links and environment keys against actual validated config.
- Record exact executed commands, pass/fail results, coverage, tested versions, preview URL, reachability scope, browser assertions and remaining limitations. Do not present planned or unavailable checks as successes.
- Verify every task's status/evidence and update the changelog. Keep PLAN-001 implementing until final delivery checks actually pass; completion must not imply unperformed main integration or user-approved design.
- Final main integration requires explicit user approval after the completed diff, review, checks and merge plan are concrete and reviewable. Material new scope also requires a concrete proposal. Delivery preparation is recorded above; final completion remains pending.

- 2026-09-07 acceptance clarification: consume SAVE-001 only after acceptance/integration. Keep the downstream chain sequential. Exercise actual API/UI/browser/binary file saves on the explicitly configured, verified Linux overlayfs root described in README/PLAN-004; test real unsupported-root refusal separately. Preserve external direct edits and dirty drafts, expose filesystem_unsupported without force-save or automatic retry, and do not relabel the retained after-final-validation diagnostic as passing application safety. PLAN-001's polling contract governs refresh; older watcher/event wording above does not add a streaming subsystem.

- Delivery dependency clarification: consume TEST-001 -> NATIVE-001 -> CI-001 -> REVIEW-001 evidence. Document ordinary native and executable acceptance independently from current overlay checks, with actual filesystem/device/runtime/commit and hosted run URL when available. The test runner supports explicit disposable existing-service URLs; this preparation alone is not native or binary success.

- CI/release acceptance (authorized 2026-09-07): inspect PLAN-002, immutable action pins, least privilege, strict tag-derived versions, draft/rerun safety, compile failure cleanup, checksums and complete embedded-asset inventory. Require actual binary-only traced runtime independence and authenticated browser save/conflict/byte preservation on the documented filesystem. Downloaded executables require no Bun/Node/source/frontend directory. Native Linux x64 and actual hosted run/publication status are separate evidence gates; preparation or local workflow validation cannot establish them. First release version remains unchosen and prototype needs-review.

### Verified bounded preparation (2026-09-07 23:05)

- Corrected the P3 drift in AGENTS, architecture and README: working authenticated editor/renderer, actual SPA route behavior, complete `check:ci` prerequisites and implemented executable embedding. Added source-versus-executable setup, silent/private token handling, original-root external editing, Markdown/renderer limits, explicit storage admission versus acceptance, local-only reachability and actual failed hosted status. `.env.example` changes are comments only. No active review record or historical owner/status/result was rewritten. The documentation hygiene scan found no remaining internal absolute scratch identifiers in the other public records, so no historical record changes were needed.
- Built and exercised source `4ca471274d3ffa76469f8b2e87d4abd7055aa764` with documentation changes only. Fresh ignored local Bun installation used `npm install --prefix .cache/runtime --no-save --package-lock=false bun@1.4.2`; `bun --version` returned `1.4.2` (744846f84), `node --version` returned `v24.20.0`; host Linux ARM64. Both frozen dependency installations were new in this checkout. No global runtime/tool changes occurred.
- Fixture identity was checked before copying samples: exclusive canonical project-specific `/tmp` parent, overlay `0x794c7630`, device `70`, descriptor mount `318`, mount device `0:70`; own ignored `tmp/doc/refused-parent`, host-shared `0x6a656a63`, device `41`, descriptor mount `331`, mount device `0:44`. The observed shared mount metadata mismatch remains refused. Exact private local paths are retained in `tmp/doc/environment.json`; they are not operator project locations or native ext4 evidence.

| Executed command | Actual result and evidence |
| --- | --- |
| `bun install --frozen-lockfile && bun install --cwd web --frozen-lockfile && bun run build` after the pinned setup above | Exit 0; `tmp/doc/setup.log`, `setup.exit`. Both locks unchanged; production API/SPA built. Vite's existing greater-than-500-kB advisory remains. |
| `NODE_ENV=production DIAGRAMDOCK_API_MODE=prefixed bun run start` | Real built source listener in an owned tmux window, with explicit root/token/host/port/origin exported by private `tmp/doc/production.sh`. HTTP health success; no nsl or Vite process in this production service. |
| `bun tests/integration/api/smoke.ts` | Exit 0; `tmp/doc/http.sh`, `http.log`, `http.exit`. Explicit expected overlay type, source URL/root and owner-only token file. Login/tree/independent blocks/save, exact surrounding bytes, external revision, 409 conflict, local assets/404 and logout/401 passed; temporary private HTTP request files removed and Markdown restored. |
| `PLAYWRIGHT_BROWSERS_PATH="$PWD/.cache/playwright" web/node_modules/.bin/playwright install chromium` | Exit 0; project-local Chromium/headless shell 153.0.8010.12, Playwright 1.63.0. `tmp/doc/browser-install.log`, `browser-install.exit`. |
| `bun run --cwd web test:e2e -- workspace.spec.ts` | Exit 0, 4/4 cases in 21.6 seconds, no retries or unexpected console/page/network errors; `tmp/doc/browser.sh`, `browser.log`, `browser.exit`. Uses the two explicit source-service URLs/root/token and `DIAGRAMDOCK_TEST_EXPECTED_FS=0x794c7630`. Real Markdown/snapshot saves, external refresh/atomic replacement/conflict/deletion, dirty/session recovery, desktop/narrow views, hostile rendering rejection and unsupported save 503 passed. |
| `curl -fsS "$DIAGRAMDOCK_DESIGN_URL" -o tmp/doc/served-prototype.html` then `cmp designs/diagramdock/DiagramDock.html tmp/doc/served-prototype.html` and `bun run designs/diagramdock/check.ts` | Exit 0; `tmp/doc/design-check.sh`, `design-check.log`, `design-check.exit`, `tmp/design/check-results.json`. Nine verification groups, HTTP resource closure and offline rendering passed with zero console/runtime/external-request errors. No prototype rebuild or metadata modification. |
| `bun tmp/doc/verify-docs.ts` and `git diff --check` | Passed; final `tmp/doc/document-validation.json`. Checks relative document links/anchors, Bash block syntax, all 15 runtime environment keys, secret absence in text logs, allowed changed paths and preserved source/locks/designs/samples/symlink. An initial scratch hygiene expression falsely matched the relative `features/workspace/` path; restricting it to absolute-path boundaries resolved that scanner error without any source change. |

- Screenshots actually inspected: `tmp/ui-desktop-light.png` (1440x920), `tmp/ui-narrow-preview.png` (390x844) and `tmp/design/desktop-light.png` (1440x960). The browser checks also retained `tmp/ui-desktop-dark.png`, `tmp/ui-conflict.png`, `tmp/ui-narrow-source.png`, `tmp/ui-narrow-files.png` and prototype state images under `tmp/design/`. The compact header, 232 px explorer, monospace source, split preview and narrow tabs match the reference. Persistence/authentication in the prototype remain simulated; metadata remains `needs-review`, `designSystems: []`, with no user design approval.
- This preparation did not rerun unchanged `check:ci`, collect new coverage or compile a new executable. Historical ext4-candidate source/executable results at `1a65402ed63c1b0702ad1de3598d9d4b3f170397`, frontend-correction results at `4fa0938cd262ea151a9b14efb0b04d669eb28137`, and the original independent review at `c1078e302cfe43eeaa8fc5bf9286a09273a74ee5` remain separate. NATIVE-001 records 152 backend tests / 98.94% function / 99.47% line coverage and 19 executable browser cases at its candidate. UI-001 records 54 frontend tests / 91.76% statements / 88.19% branches / 89.16% functions / 92.41% lines and 24 executable browser cases at its correction. Those counts are historical and do not establish a combined native/x64 result.

### Preview lifecycle and review boundary

- Retained source preview: `http://127.0.0.1:42927`, loopback only, built source at the baseline above and unchanged copied sample bytes. Private access token remains in ignored `tmp/doc/token`, mode `0600`; never print it or include it in reports. `tmp/doc/production.sh` reads that file without putting the value on the command line. `tmp/doc/environment.json` identifies the retained owned root and `tmp/doc/lifecycle.json` verifies the final state.
- Retained prototype: `http://diagramdock-design-4984b3.localhost:3003/diagramdock/DiagramDock.html`, wrapped through supported `nsl run`. Its own listener binds `127.0.0.1:26214`; the pre-existing shared nsl proxy binds `0.0.0.0:3003`. HTTP/browser checks verified only local-machine access. No LAN/public route, tunnel, deployment or remote access claim follows from these observations.
- The refusal source URL was `http://127.0.0.1:39689`. It was stopped through tmux before removing its exclusive fixtures/script, and HTTP unreachability was independently checked. The positive sample files match committed examples; no test service remains except the explicitly retained source preview. No browser process or credential-bearing trace is retained.
- From the repository root, derive `session_name="$(basename "$PWD" | tr '.' '-')-$(echo -n "$PWD" | md5sum | cut -c1-6)"`. Stop retained previews with `tmux send-keys -t "$session_name:production" C-c` and `tmux send-keys -t "$session_name:doc-design" C-c`; leave the shared nsl daemon intact. Verify listener shutdown before removing the exact owned demo root recorded in `tmp/doc/environment.json` and the token/private launcher. Do not remove operator roots or unrelated sessions.
- Documentation self-review applied the shared review policy to the changed prose, source contracts, script invocations, evidence scopes and limits. No remaining high-confidence introduced documentation issue was found. The concrete P3 correction is ready for independent review; that finding is not marked resolved in the active review record by this task. Final corrected review and actual hosted ext4/Linux x64 source/executable/browser acceptance remain outstanding. The first hosted run `34166805672` remains failed, first release version unchosen, no published release verified, and the final-window `applicationSafetyPassed=false` diagnostic remains unchanged.
- Bounded preparation is complete. DOC-001 stays `in_progress` and PLAN-001 stays `implementing` pending the final evidence-only update. No other task status, main integration, remote push, tag, release or design approval was performed.

### Supplied evidence update: investigation and proposal (2026-09-07)

- Continued the existing claim from clean documentation commit `c817d541a2a52098209da9806e91b015771f4335`; branch, origin and prior evidence are preserved. No integration sync or active REVIEW-001 edit is part of this update.
- Newly supplied independent review reports P2 resolved at `e70dd9c`: the retained five-case baseline had two expected failures; the corrected five browser cases and 25 focused tests passed, including draft/byte protections and the session-cache correction. Review report `a50e53c` is integrated at `3cfe519605a15e43d937c5269794ccc4f76c3ba1`; the supplied comparison with the required source foundation changes only REVIEW-001. This is supplied provenance, not a new test execution here.
- Newly supplied public run/job/artifact inspection reports [run 34168525432, attempt 1](https://github.com/itxje/diagramdock/actions/runs/34168525432) completed successfully at exact candidate `4ca471274d3ffa76469f8b2e87d4abd7055aa764`. The native source/executable acceptance step and checked executable retention succeeded; publication was skipped. Artifact `10034988497`, `checked-linux-x64-4ca471274d3ffa76469f8b2e87d4abd7055aa764`, was present and not expired at that inspection. Detailed native filesystem identity, counts, hashes, traces and cleanup evidence have not been supplied for this documentation update.
- Proposal under the existing authorization: update current public status to distinguish successful hosted metadata from pending detailed native/executable evidence review; record P2 resolution provenance and preserve P3 independent confirmation, the first failed run and all historical local checks. Keep DOC in progress, PLAN-001 implementing and the prototype needs-review. Verify only changed documentation links, shell/config consistency, preservation and whitespace; do not repeat unchanged gates or infer detailed acceptance from the green job.

### Evidence update outcome (2026-09-07 23:08)

Current README, AGENTS, SECURITY, architecture and environment comments now distinguish successful hosted metadata from pending detailed evidence review. The supplied P2 resolution, exact candidate/run/attempt, artifact identity and skipped publication are recorded without changing the active review file or inferring native counts, filesystem/device, binary hashes or trace/cleanup results. Earlier dated pending notes remain the prior evidence state; the first failure is unchanged. DOC-001 remains in progress and PLAN-001 implementing.

Executed only `bun tmp/doc/verify-evidence-update.ts` (exit 0; `tmp/doc/evidence-update-validation.log`, `.exit`, `.json`) and `git diff --check` for this evidence-only change. The focused documentation check verified 33 Markdown documents, 105 relative links/anchors, 19 Bash blocks and all 15 runtime configuration keys, plus allowed paths, secret absence in retained logs, source/lock/design/sample preservation and the unchanged symlink. The prior validation result was preserved separately. No installation, source/browser/coverage/compile/native/raw test or remote operation was repeated. Detailed successful-run evidence review, native-policy review and P3 independent confirmation remain the concrete delivery dependencies; the existing preview lifecycle is unchanged and was not reverified in this update.

### Native evidence reconciliation: investigation and proposal (2026-09-07)

- Continued the existing claim from clean commit `303b3469b8a9d41ef2e9ed365765b4fab31f916b`, without a new merge or changes to other task ownership. Authorized read-only successful-run records and the downloaded package are now available. Initial inspection confirms source stages, binary aggregate, manifest and independent package verification for exact candidate `4ca471274d3ffa76469f8b2e87d4abd7055aa764`. The supplied native/Linux x64 evidence gate is satisfied; P3 independent documentation review still governs final DOC completion.
- Proposal under the existing authorization: inspect the structured run/log records, verify their identity and stage/aggregate relationships, and check the already downloaded package's digest/manifest/ELF without executing it on the local ARM64 host. Reconcile current setup/storage/release/status prose with the verified execution. Preserve dated pending/failure history, practical final-write-window and local OS actor limits, source-stage pending labels, needs-review prototype and final main-integration boundary. Do not redownload artifacts or repeat CI, native, browser or raw diagnostics.
- Evidence limits: successful individual raw probe result files and raw syscall traces were not uploaded. Record actual emitted stage/audit summaries and their caller semantics, never a personal replay of those missing files or a query of the hosted test listeners. Keep internal absolute evidence locations out of public documentation; store only neutral extracted verification results in own ignored `tmp/doc/`.

### Verified native evidence outcome (2026-09-07 23:15)

- Read the supplied actual run log and structured records for [run 34168525432, attempt 1](https://github.com/itxje/diagramdock/actions/runs/34168525432), clean candidate `4ca471274d3ffa76469f8b2e87d4abd7055aa764`. Both supplied log copies have SHA-256 `4e83324b6d688bcf1490187a5cf702f71d06ac4f5d69137d83887516ee2445d0`. Parsed the actual log and checked ordered stage results, aggregate fields, observed filesystem identities and complete package identity. No artifact was downloaded again and the x64 executable was not run on this ARM64 host.
- Native source: Linux x64 / Bun 1.4.2; actual ext4 `0xef53`, device `2049`, descriptor mount `27` / `8:1`, with separate tmpfs refusal `0x1021994`, device `26`, mount `32` / `0:26`. The twenty raw stages and file/HTTP stages all exited zero without signals; nativeSourceChecks passed and cleanup true. The separate tmpfs target rejection is an expected negative test. Source-stage nativeAcceptance pending remains intact and is superseded by the complete executable aggregate. Bun's reported Node compatibility version is not an independently executed Node toolchain.
- Actual log summaries: 152 backend passes, 54 frontend passes, 18 release passes; lint/types/build/workflow gates passed. Backend coverage was 98.94% functions / 99.47% lines; frontend coverage was 91.76% statements / 88.19% branches / 89.16% functions / 92.41% lines. These counts overlap focused suites and are not summed. The full source/release gate includes induced compile failures and cleanup tests; no such checks were rerun for this prose update.
- Complete binary aggregate: `result=passed`, `nativeAcceptance=passed`, `cleanup=true`; actual Linux x64 target with 24 browser cases in 1.2 minutes, 93/93 assets, flowchart/sequence/class/state lazy families, zero unexpected errors/external requests. Emitted strace 6.8 audits record one execution per service, empty runtime PATH, zero checkout accesses and no frontend extraction. Refusal audit: 3 paired calls / 404 accesses / 0 writes. Supported audit: 251 paired calls / 19,833 accesses / 16 scoped writes. The runner reverses its service list during confirmed shutdown, so the refusal audit appears first. Hosted origins on loopback ports 38641/37589 were test-only, not queried here or retained as previews.
- Inspected `scripts/smoke-release.ts` and `scripts/release/trace.ts` to interpret those summaries: copied-binary-only runtime directory, actual version/build-info matching, full asset transport/hash checks, browser disk/auth/conflict checks, checked process shutdown/listener closure, scope-aware syscall audit and fixture cleanup precede the aggregate pass. Successful raw syscall files and individual raw probe JSON were not uploaded. This assessment does not claim replay or direct inspection of missing raw evidence. The historical final-window `applicationSafetyPassed=false` diagnostic remains unchanged.
- Independently validated the supplied package using the existing read-only `releaseFiles` validator and `readelf -h -l -d`: exact directory inventory, strict manifest, tag/version/commit/target, 93 assets, matching SHA256SUMS and x86-64 ELF. Executable `diagramdock-0.0.0-ci.fixture-linux-x64`: 85,485,024 bytes, SHA-256 `9be56bbd7551bdd27806571a133112947aee2deb27b6dbdce3f030c71337f354`, Bun 1.4.2, fixture tag `v0.0.0-ci.fixture`. Earlier two-asset mocked release-test outputs in the log are not this package. The observed ELF interpreter is `/lib64/ld-linux-x86-64.so.2`, with libc, loader, libpthread, libdl and libm dependencies; embedded runtime/resources are not a static/all-OS promise.
- Artifact metadata identifies `10034988497`, `checked-linux-x64-4ca471274d3ffa76469f8b2e87d4abd7055aa764`, unexpired in the supplied snapshot. Archive size 85,506,772 bytes / digest `dc0b9dd49f04f5bdb86fb8156ac4d9252627378eff9f7ba2c059d3339f402b5e` is supplied artifact metadata, distinct from the personally verified executable digest. Publication was skipped; fixture metadata does not create a Git tag or choose the first release version.

Executed focused commands for this evidence reconciliation:

| Command | Result and retained evidence |
| --- | --- |
| `bun tmp/doc/verify-native-evidence.ts` | Exit 0; `tmp/doc/native-evidence.log`, `.exit`, `.json`. Assertions over supplied actual log/package and existing read-only manifest validator; no local binary execution or remote operation. |
| `bun tmp/doc/verify-native-preview.ts` | Exit 0; `tmp/doc/native-preview-state.log`, `.exit`, `.json`. Source health/page and served prototype exact bytes, retained overlay identity, unchanged copied samples, token mode 0600, explicit listener binds and refusal service shutdown verified. No browser rerun; earlier inspected screenshots remain the visual evidence. |
| `bun tmp/doc/verify-native-evidence-docs.ts` and `git diff --check` | Documentation links/anchors, Bash syntax, configuration names, allowed paths, credential redaction and preserved source/locks/designs/samples/symlink; exact results in `tmp/doc/native-document-validation.log`, `.exit`, `.json`. |

Current README, AGENTS, SECURITY, environment comments and architecture now record verified native execution and package identity instead of metadata-only pending claims. Dated earlier evidence states remain unchanged. The useful source/prototype previews and owner-readable ignored token remain as described in the lifecycle section; this update rechecked only their HTTP/content/storage/listener state. No additional service, browser session or test fixture was created.

The supplied native/Linux x64 gate is satisfied. Supplied P2 and policy review are passed; the P3 documentation corrections are complete and ready for independent confirmation. DOC-001 remains `in_progress` and PLAN-001 remains `implementing` until that final review; no other task status/owner or active review file was edited. No source/automation change, repeated full gate, remote push/tag/release, main integration or design approval occurred.

### Final closure: investigation and proposal (2026-09-07)

- Continued and re-read the existing Documentation maintainer claim and in-progress index marker after a clean local synchronization to `eabc4d7263156a343762d2e50e573037f6f5f3c8`. The accepted final review `a0f0f039ed20fd3289717898a8f8d4a349db0555` records PASS, P2/P3 resolved and no remaining actionable findings; P3 was reviewed at documentation `7d6d720`. The unchanged native source/executable and package acceptance remains tied to `4ca471274d3ffa76469f8b2e87d4abd7055aa764` and run 34168525432.
- Proposal under the original 2026-09-07 implementation and delivery authorizations: complete this documentation task and the reviewed implementation deliverable in PLAN-001, while retaining the separate final main-integration approval boundary. Reconcile only current status prose, preserving earlier dated failure/pending evidence, needs-review prototype and unchosen first release. No new approval event is recorded.
- The explicitly authorized administrative disposition closes FILE-001 as superseded by completed SAVE-001. Preserve its neutral owner, original in-progress result, previous completion timestamp, exhausted correction history, unresolved shared-mount cause and conservative checks. Closure is not a passing result or acceptance waiver.
- Verify only read-only preview HTTP/content/listeners/copied samples/token mode and focused documentation/status/scope preservation against the synchronized baseline. Do not read token contents, repeat browser/application/native checks, redownload artifacts or alter the completed review. The accepted review requires no additional cycle solely for these completion markers.

### Final completion (2026-09-07 23:26)

Accepted final review `a0f0f039ed20fd3289717898a8f8d4a349db0555` is integrated at `eabc4d7263156a343762d2e50e573037f6f5f3c8`: PASS, P2/P3 resolved, no remaining actionable findings. P3 documentation was reviewed at `7d6d720`; status-only closure requires no new review cycle. Native source/executable acceptance remains verified by run 34168525432 at exact clean `4ca471274d3ffa76469f8b2e87d4abd7055aa764`, with the personally checked x64 package identity, filesystem scope and raw-evidence limits recorded above. No acceptance run or artifact download was repeated.

The earlier fresh frozen installs/build, real HTTP smoke, four source browser cases and nine unchanged prototype groups remain the setup/visual evidence. Final read-only `bun tmp/doc/verify-closure-preview.ts` passed: source `http://127.0.0.1:42927`, unchanged copied samples on overlay `0x794c7630` / device `70`, and the retained prototype `http://diagramdock-design-4984b3.localhost:3003/diagramdock/DiagramDock.html`. Both are local-machine previews; the source listener is loopback and the prototype uses the existing nsl proxy described above. Token mode remains 0600 at ignored `tmp/doc/token`; its contents were not read or output. The refusal listener remains stopped. `tmp/doc/closure-preview-state.{json,log,exit}` records this check. Earlier screenshots and owner-scoped lifecycle instructions remain unchanged; no browser or remote reachability check was repeated. This local source preview is distinct from the actual hosted x64 executable tests.

Final `bun tmp/doc/verify-closure-docs.ts` and `git diff --check` verify document links/anchors, unchanged Bash examples/configuration, authorized status markers and changed paths, exact preservation of source/tests/scripts/workflows/locks/designs/examples, completed REVIEW-001 and the symlink against the synchronized baseline. Results are in `tmp/doc/closure-document-validation.{json,log,exit}`. The checks use own Bun 1.4.2 with actual Node v24.20.0 and no runtime/tool changes.

DOC-001 and PLAN-001's reviewed implementation deliverable are completed. FILE-001 is closed as superseded by completed SAVE-001, not passed, with all earlier failed/exhausted evidence and unresolved shared-mount cause preserved. Other owners/statuses are unchanged. Runtime/root/token setup and safe remote-access instructions remain in README; the practical final-write window, local OS actor limits, Linux dynamic-library/procfs/storage requirements and unavailable hosted raw trace/probe files remain explicit. The first hosted failure and applicationSafetyPassed=false diagnostic remain history. Prototype status is needs-review; first release version is unchosen, no tag or release is published, and final main-integration approval is still a separate pending decision. No new approvedAt event, main merge, deployment or design approval is recorded.
