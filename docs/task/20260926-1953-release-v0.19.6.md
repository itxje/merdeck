# 20260926-1953-release-v0.19.6 Release v0.19.6

- **status**: in_progress
- **priority**: P1
- **owner**: release/session-20260926-v0196
- **createdAt**: 2026-09-26 19:53

## Description

Publish the mobile contents positioning correction and unified wider document layout as v0.19.6 through the standard native-gated release workflow. Verify downloaded checksums and embedded build identity.

## ActiveForm

Preparing the release candidate and native verification.

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

- The user explicitly requested release after confirming that the running v0.19.5 does not include the mobile contents fix. This authorizes pushing the reviewed changes, tagging and publishing.
- The combined code at `856be92dc789787983b3c407918a72242d76cbb0` already passed clean-source local `check:ci`, with 582 frontend cases and two 98-case browser runs (17 configured skips each). Later commits only update tracking documentation. Exact release-commit Linux x64/ext4 acceptance remains required before tagging.
- UI corrections are patch releases under repository policy. The latest published version is v0.19.5.
- Publication does not update a running service or establish physical-iPhone acceptance.

## Native verification progress

- Release candidate: `10ac759f574a3d69a7e6c77d41730adf694480c5`, pushed to main. The implementation diff since locally accepted `856be92` is empty; changes are tracking documentation only.
- [Native workflow](https://github.com/itxje/merdeck/actions/runs/36267598729) attempt 1 failed before application acceptance, in tool/browser setup. The runner reported `getaddrinfo EAI_AGAIN` while restoring its cache. Setup logs were unavailable in the downloaded log, so that warning does not establish the exact setup failure. The subsequent evidence export also failed because no test scratch directory existed. Attempt 2 reruns identical bytes with all checks unchanged. Evidence: `tmp/release-v0196-native-first.log` and `tmp/release-v0196-native-failed.log`.

- Native attempt 2 reached application tests and exposed a timing race in the contents-toggle unit test: navigation could become observable before the initial path-reset effect, so the test set scrollTop=200 before initialization reset it to zero. The corrected test observes and awaits the initial reset before simulating user scrolling, then also asserts toggling never calls reset again. No assertion is removed and production code is unchanged. The release candidate will advance before tagging and rerun native acceptance; no v0.19.6 tag exists yet.

- The synchronization correction passes all 36 document renderer unit cases, frontend lint and typecheck, and the complete 582-case frontend suite with coverage. Review confirms the mock waits for a real initialization signal and retains the article identity and scroll-position assertions; no production code changed. Local evidence: `tmp/release-v0196-scroll-unit.log` and `tmp/release-v0196-frontend.log`.
