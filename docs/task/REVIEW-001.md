# REVIEW-001 Review the complete implementation

- **status**: completed
- **priority**: P1
- **owner**: Implementation reviewer
- **createdAt**: 2026-09-07 17:42

## Description

Review the complete implementation using the implementation-review skill and applicable TypeScript backend/frontend references. Assess correctness, security, maintainability, UI stack compliance and acceptance evidence against [PLAN-001](../plan/PLAN-001.md). Resolve material findings within the authorized scope before delivery, then rerun affected checks.

## ActiveForm

Reviewing the complete implementation

## Dependencies

- **blocked by**: CI-001 (after TEST-001 and NATIVE-001 preparation)
- **blocks**: DOC-001

## Notes

### Review investigation and proposal (2026-09-07)

- Claimed and re-read the matching index/detail before investigation. Reviewed baseline: `c1078e302cfe43eeaa8fc5bf9286a09273a74ee5`, compared with empty initialization `0eb0827`. The initial worktree was clean. Existing authorization in PLAN-001 and PLAN-002 covers this bounded implementation review; no later approval is inferred.
- Investigation: the integrated repository includes file, HTTP, editor, storage-preparation and executable/automation implementations. Local overlay evidence is distinct from pending native filesystem and hosted Linux x64 execution. The current implementation intentionally refuses other write filesystems. Design metadata remains needs-review with no imported design system.
- Proposal: inspect the full implementation and relevant tests, prioritize containment/data integrity/auth/async rendering and release publication boundaries, execute independent frozen installs and `bun run check:ci`, inspect actual browser/asset/trace/cleanup evidence, and reproduce only high-confidence suspected defects. Preserve the practical final comparison/rename and local OS actor limitations.
- Scope: author only this task and its exact index marker; keep disposable reproduction evidence in ignored project scratch. Report actionable corrections for their owning implementation tasks without changing application, tests, workflows, dependencies or shared documentation. Keep this task in progress when blocking findings or required delivery evidence remain. This review does not approve final integration or publication.

### P2 correction review investigation and proposal (2026-09-07)

- Continued and re-read the existing Implementation reviewer / in_progress claim and matching index marker. The clean worktree was synchronized to accepted commit `e70dd9c5779d8c1893c624f59480b9ab0832f316`; the previous review notes remain intact. Existing authorization covers this bounded correction review, with no new design or delivery approval.
- Investigation scope: compare the correction with `c1078e302cfe43eeaa8fc5bf9286a09273a74ee5`, including structured warning reconciliation, revision-cache ordering, session-expiry eviction and adjacent reducer/hook/browser regressions. Assess committed-save response ordering, preserved sibling/newer drafts and distinct external/deletion/auth controls.
- Proposal: run scoped frontend lint/types and focused reducer/hook tests, rebuild the integrated source and execute the five actual-service committed-save browser cases with independently identified disposable supported/refused fixtures. Inspect response sequencing, byte equality, unexpected-error audit and cleanup; establish the regression against the baseline without altering tracked application/tests. Preserve previous failed reproduction evidence and distinguish these built-source checks from supplied executable verification.
- Limits: reuse the completed full audit and immutable action/security review. Do not repeat unrelated aggregate/native investigations. Only this task and its own index marker may change; overall review remains in progress for documentation and real corrected native/Linux x64 acceptance, regardless of the P2 outcome.

### Native policy review investigation and proposal (2026-09-07)

- Continued and re-read the existing Implementation reviewer / in_progress claim and matching index marker. Synchronized the clean worktree to accepted integration `3cfe519605a15e43d937c5269794ccc4f76c3ba1`. Its code/config/tests match full-gate candidate `4ca471274d3ffa76469f8b2e87d4abd7055aa764`; only this review record differs. Previous P2 resolution and failure evidence remain intact. Original authorization covers this bounded policy review.
- Investigation scope: compare the native admission change with `e70dd9c5779d8c1893c624f59480b9ab0832f316`, including held-descriptor mount association, filesystem name/magic/device checks, fail-closed errors, handle lifetime, refusal-before-temp coverage and storage-runner/API expectations. Inspect PLAN-005 and the explicitly supplied historical native failure artifacts; compare parser/device encoding with primary Linux/glibc sources.
- Proposal: run root lint/types, the strict storage test project and focused filesystem/storage tests using the existing pinned local runtime and independently identified disposable overlay/shared fixtures. Verify actual case counts, errors and cleanup. Record code-review conclusions separately from supplied combined executable evidence and pending real corrected ext4/Linux x64 acceptance.
- Limits: author only this task and its own index marker, preserve the P2 resolution, and keep overall review in progress. Do not repeat unrelated full gates, binary/browser checks or raw native research; documentation correction and real hosted acceptance remain separate work.

### Native artifact evidence investigation and proposal (2026-09-07)

- Continued and re-read the existing Implementation reviewer / in_progress claim from clean tracking commit `b04113b2194579b5cc859e1983b9867f01d02496`, with source unchanged. Existing authorization now provides the downloaded checked executable, checksum, manifest and bounded successful-run records for read-only verification.
- Investigation: link the supplied artifact to the exact previously reviewed candidate/run, distinguish its executable digest from the transport archive digest, and check the actual ELF target and strict release metadata against the current validator. Successful hosted raw child results and syscall traces were not uploaded; the previously inspected execution summaries retain that limit.
- Proposal: hash and stat the supplied executable, validate the exact package inventory/checksum/manifest without executing it, inspect its ELF interpreter/library requirements, and compare the supplied source/smoke summaries with the independently parsed log. Record the native evidence decision and leave only unaccepted documentation review pending. No download, source change, aggregate rerun, local x64 execution or new integration is part of this follow-up.

### Final documentation investigation and proposal (2026-09-07)

- Continued and re-read the existing Implementation reviewer / in_progress claim and matching index marker. The clean branch synchronized only the accepted local integration at `7d6d720a37123d200463ac6473fdafe48964d8c2`; prior review records and origin were preserved. Existing authorization covers this final documentation assessment without authorizing main integration or publication.
- Investigation: compare the accepted documentation delta from `409df49` with adjacent implementation/scripts and the already accepted native candidate `4ca471274d3ffa76469f8b2e87d4abd7055aa764`. Verify current setup, configuration, authentication, storage, executable/platform, historical/current evidence and prototype claims; inspect the explicitly supplied documentation checks and preview records.
- Proposal: independently verify source/configuration-value preservation, changed-document relative links/anchors and Bash fence syntax with an owned ignored TypeScript checker, inspect setup/HTTP/browser/preview evidence, and resolve P3 if consistent. Run no unchanged application gate or executable/browser/native tests. Complete only this review task and its index marker if no actionable finding remains; later documentation/plan status-only completion does not block the review.

### Confirmed findings

- **P2 / MEDIUM (resolved at `e70dd9c`): own-save polling race leaves a false external-change lock.** Original finding at `c1078e3`: `web/src/features/workspace/drafts.ts:60-64,71-84`, with save guards at `use-workspace.ts:136` and `workspace.tsx:33`. A revision response can observe the committed save while its response is still in flight. The reducer records an external-change warning; successful reconciliation updates the baseline but preserves that warning. Subsequent typing cannot be saved until an unnecessary explicit review, even with no external writer. Reproduction: `.cache/review-tools/bun/package/bin/bun tmp/review/save-race.ts` (exit 0), `tmp/review/save-race.json`: saved baseline matches, newer draft stays dirty, save remains blocked. Correction: retain structured observed revision/deletion state and clear only a warning matching the successful own-save revision; preserve distinct external changes/deletion/auth locks. Add a controlled response-order regression and a distinct-external-version negative control. The independent correction assessment below resolves this finding while retaining its original evidence; no source changes made here.
- Browser confirmation: `PATH="$PWD/.cache/review-tools/bun/package/bin:/usr/bin:/bin:/usr/local/bin" PLAYWRIGHT_BROWSERS_PATH="$PWD/.cache/playwright" bun tmp/review/browser-race.ts` exited 0. The real built service committed a save before the intercepted response was released; real revision polling then produced the warning. After response delivery, disk bytes matched the submitted source and newer local text remained intact, but Save stayed disabled. `tmp/review/browser-race.json` records `externalWrites: 0`, `diskMatchesSubmitted: true`, `draftPreserved: true`, `nextSaveBlocked: true`, no unexpected browser errors and successful cleanup. Inspected `tmp/review/save-race.png`. The existing save-delay browser case delays before backend execution, so it misses this ordering.
- **P3 / LOW (resolved at `7d6d720`): contradictory implementation and verification instructions.** Original finding at `c1078e3`: `AGENTS.md:12,14`, `docs/architecture.md:5,84,143,147` and `README.md:85` described a health-only/starter or pending editor, a JSON 404 at `/`, an incomplete quality command, or pending executable embedding. The implemented service served the authenticated editor at `/` and the executed gate exercised the embedded executable. The stale gate omitted automation/executable acceptance and mixed deployment descriptions obscured the remaining native gate. The accepted documentation now matches the implemented routes, complete setup/gate, source/executable requirements and verified native execution, with explicit chronology, limits and needs-review design. The independent final assessment below closes this finding without erasing its original evidence.

### Independent local review evidence (2026-09-07)

- Scope: reviewed the complete tracked initial implementation relative to `0eb0827`, including source, manifests/locks, workflows, scripts, tests, design reference and surrounding architecture/acceptance decisions. Source reviewed and built: `c1078e302cfe43eeaa8fc5bf9286a09273a74ee5`. During checks, only this task and its index claim were modified; application/configuration/test/lock content was unchanged. The resulting executable manifest records that source commit. This is a finite local review, not final delivery approval.
- Tools: project-local integrity-verified Bun 1.4.2, actual Node 24.20.0, matching local Playwright 1.63.0 Chromium, actionlint 1.7.12 and Debian ARM64 strace 6.13. Checked the live package metadata before extraction; the strace package SHA-256 was `d793af70a104eb0bd5be466c3043b512f549e7cb27971320a7c9b8f4c6165e55`. `bun scripts/setup-ci-tools.ts` verified the actionlint archive. Provisioning evidence is under `tmp/review/`; no global tools were changed.
- Actual disposable fixture identities: exclusive canonical `/tmp` parent, Linux overlayfs `0x794c7630`, device `70`; separate own `tmp/` refusal parent, `0x6a656a63`, device `41`. Verified both before the gate; exact paths are in `tmp/review/fixture-identities.json`. These sample copies do not establish ordinary native filesystem support or relocate operator files.
- Executed the following complete command once in the project tmux session through `tmp/review/full-gate.sh`. `tmp/review/full-gate.exit` is `0`; inspected the actual full log and nested structured results. The wrapper captures the AND-list status immediately, before any cleanup can mask a failure.

```bash
export PATH="$PWD/.cache/review-tools/bun/package/bin:/usr/bin:/bin:/usr/local/bin"
export DIAGRAMDOCK_TEST_FIXTURE_PARENT="$(cat tmp/review/fixture-parent.txt)"
export DIAGRAMDOCK_TEST_EXPECTED_FS=0x794c7630
export DIAGRAMDOCK_TEST_UNSUPPORTED_PARENT="$PWD/tmp"
export DIAGRAMDOCK_TEST_UNSUPPORTED_FS=0x6a656a63
export PLAYWRIGHT_BROWSERS_PATH="$PWD/.cache/playwright"
export DIAGRAMDOCK_STRACE="$PWD/.cache/review-tools/strace/usr/bin/strace"
bun install --frozen-lockfile && bun install --cwd web --frozen-lockfile && bun run check:ci && git diff --check
```

| Check | Observed result |
| --- | --- |
| Frozen root/web installs, lint, strict types and production build | Passed; Vite's greater-than-500-kB advisory remains |
| Workflow lint | One workflow passed; six immutable action pins also matched their official repository tags through an independent read-only API check |
| File acceptance | 85 passed, 664 assertions; separate domain smoke passed |
| Storage preparation | 6 passed, 54 assertions; native refusal remains a recorded failure, not native success |
| Missing-procfs isolation | 1 passed, 4 assertions; descriptor-resolution ENOENT fault injection, not a physically unmounted-procfs experiment |
| Backend | 122 passed, 1,172 assertions; 99.57% function / 99.44% line coverage |
| Frontend | 43 passed in 9 files; 91.97% statement / 88.66% branch / 88.79% function / 92.68% line coverage |
| Release and CI regressions | 18 passed, 97 assertions, including all 9 interleaved trace-parser regressions and induced compile failure/interruption cleanup |
| Actual standalone executable browser suite | 19 passed in 47.9 seconds, no reported unexpected errors |
| Embedded assets and lazy rendering | 93 of 93 assets verified; flowchart, sequence, class and state loaded with no external requests |

- Test groups overlap: file acceptance is also discovered through the backend bridge; the counts are not a sum of unique tests. Frontend unit coverage excludes sourced primitives and presentational route/layout composition; backend coverage excludes the entrypoint. Browser and executable checks exercise selected assembled behavior, not every rendering syntax or possible race. The additional confirmed save-order reproduction above exposes a real gap despite the green aggregate gate.
- Executable: `dist/release/diagramdock-0.0.0-ci.fixture-linux-arm64`, 85,444,904 bytes, SHA-256 `1b6c2785b5636e28c94c7152a85ce6c1fd3f283a52ca2ee152de33d4ee554cef`. Manifest/checksum/version/tag agreement verified for the local fixture version, target `bun-linux-arm64`, Bun 1.4.2 and reviewed commit. This is not a selected public release version. The inventory contains 91 JavaScript assets, one CSS asset and one HTML document; no separate font asset was emitted.
- The copied executable ran outside the checkout with an empty PATH. Replayed its actual traces with the current parser: supported root had 355 correctly paired unfinished/resumed calls and 9 scoped writes; refused root had 7 pairs and 0 writes. Each executed only the binary, with zero checkout accesses and no frontend extraction. The checkout remained present; trace evidence establishes no access during these observed flows, not physical isolation of source. The original failed line-parser evidence remains failed; current thread/syscall pairing and nine focused regressions were independently inspected.
- Browser evidence: inspected desktop light/dark, narrow source/preview, conflict and lazy-render screenshots. The working interface carries the prototype's compact tree/editor/preview layout and responsive views; prototype metadata remains `needs-review`, `designSystems: []`. No user design approval or newly verified development/nsl preview is implied.
- Actual production test origins were `http://127.0.0.1:41269` (supported executable), `http://127.0.0.1:41489` (refused executable) and `http://127.0.0.1:32889` (focused save-race reproduction). They were loopback-only and are now stopped. Independently verified stopped markers, unreachable listeners, removal of fixture/token/config/runtime copies, absence of compile scratch, and removal of the empty exclusive fixture parent. Only the idle project tmux shell remains. `tmp/review/verified-evidence.json`, `tmp/release-smoke-umyRlA/result.json` and `tmp/e2e-qk0Af9/` retain local evidence; raw syscall traces remain private and must not be uploaded.

### Hosted failure evidence follow-up (2026-09-07)

- Continued the existing claim with a clean worktree at tracking commit `d14578526867d6d9cedd501794c3261180d1ce9a`. Investigation/proposal for this evidence-only follow-up: verify the supplied public run identity/conclusion, distinguish observed metadata from supplied raw-probe results, and update these Notes without changing the reviewed source or repeating unchanged local gates. The P2 correction is assigned but has not been supplied as an accepted integrated revision; the finding remains unresolved here.
- Independently verified [run 34166805672, attempt 1](https://github.com/itxje/diagramdock/actions/runs/34166805672) through the public run and attempt-jobs APIs: `.github/workflows/verify.yml`, push event, exact commit `c1078e302cfe43eeaa8fc5bf9286a09273a74ee5`, completed with **failure**. The native source/executable acceptance step failed; preparation and upload of failure reports succeeded; checked-executable retention and publication were skipped. Local metadata copies are `tmp/review/remote-run-34166805672.json` and `tmp/review/remote-jobs-34166805672.json`. This is concrete hosted failure evidence, not an unrun workflow or successful native acceptance.
- Supplied acceptance evidence reports actual Linux x64, Ubuntu image `20260831.293.1`, Bun 1.4.2 and Node 24.20.0; ext4 magic `0xef53`, device `2049`, mount `27`; refusal tmpfs `0x1021994`, device `26`, mount `32`. All twenty unchanged real ext4 raw child probes reportedly completed with zero failures/signals, no leftovers, fixture removal and `cleanup: true`. Current overlay-only production admission then refused ext4: `nativeSourceChecks: failed`; native acceptance, binary and browser execution remain pending. The raw JSON/exit records and environment details were supplied evidence, not independently downloaded or re-executed by this review; the public run identity, failure and skipped publication were independently checked.
- These raw controls do not establish application acceptance or explain shared-mount behavior. Any bounded admission correction must identify ext4 from the actual held-descriptor mount, since magic `0xef53` alone also matches ext2/ext3, while preserving existing identity/version/path/procfs/device/refusal guarantees and direct external editing. Subsequent review requires the exact accepted correction commit and a real successful `check:ci --native` covering source/file/HTTP plus Linux x64 binary-only browser/assets/trace flows. The local ARM64/overlay evidence and retained final-write-window diagnostic are unchanged; no candidate correction or active implementation branch was integrated here.

### Independent P2 correction assessment (2026-09-07)

- **P2 resolved.** Reviewed accepted integration `e70dd9c5779d8c1893c624f59480b9ab0832f316` against `c1078e302cfe43eeaa8fc5bf9286a09273a74ee5`. Its application/tests match implementation commit `4fa0938cd262ea151a9b14efb0b04d669eb28137`; only the expected frontend correction and adjacent tests changed application behavior. Backend, storage policy, scripts, dependencies and locks are unchanged. No new high-confidence regression was found in this bounded delta.
- `drafts.ts:36-49,100-118` retains one observed revision and a sticky divergent flag, so memory remains bounded. A successful save clears only a single matching own-save revision; different versions, deletion, session, layout and mutation states remain blocked. Baseline/sibling selector reconciliation retains newer local typing and separate sibling drafts. `workspace.tsx:176` renders the structured warning through its display function without changing save guards or accessibility roles.
- `use-workspace.ts:149-164` cancels pending observations after the response, rechecks the session generation, feeds the latest cached revision into reconciliation and preserves a distinct external/deleted revision instead of replacing it with the older save response. `use-workspace.ts:72-77` repeats cache eviction after expired observers disable. The session negative control verifies retained locked drafts, rejection of the late save and absence of the expired document cache entry. No polling suppression, force-save, external rebase or weakened server admission was introduced.
- Regression sensitivity was independently established before rebuilding. All 93 retained baseline frontend resources matched the prior `c1078e3` release manifest hashes/sizes; the unchanged backend build SHA-256 was `756d00a278ca792a0f46660a767b3ec758f6e6bc91f5b2c7ea2e8776a24d303f`. Running the five new actual-service browser cases against that baseline produced **2 failures / 3 passes**, exit 1 in 24.9 seconds: own-save failed at `save-race.spec.ts:131` because Review current file remained; auth-change failed at line 124 because the expected retained session warning was absent. In-place, atomic and deletion controls passed. All five browser error audits were zero. These expected baseline failures remain in `tmp/review/p2/baseline-browser.log` and `baseline-playwright/`; original review reproduction files were left unchanged.
- The browser route calls `route.fetch()` and verifies the successful receipt, submitted version and actual committed disk bytes before holding only response delivery. It observes real revision and document fetches before release; the positive case then writes the sibling with refreshed selector/full-file version and finally writes the newer first-block draft. Each negative control performs an independent in-place write, atomic replacement, deletion or authentication change while the response is held. No fixed sleep, mocked success response, ignored polling or force-save can account for the passing corrected cases.
- Independent environment: existing pinned Bun 1.4.2, actual Node 24.20.0 and local matching Chromium in the project tmux session. Verified a new exclusive canonical disposable `/tmp` parent as overlayfs `0x794c7630`, device `70`, and own ignored `tmp/` as the distinct refused `0x6a656a63`, device `41`. Exact identities and baseline provenance are in `tmp/review/p2/preparation.json`. Only disposable sample copies were used.
- Executed commands are recorded below. Baseline and corrected browser runs use separate output directories; the focused unit subset alone disables coverage, as authorized. Full unchanged coverage/executable results are supplied evidence, not an independent rerun here. Each command status was captured immediately before later commands or cleanup.

```bash
export PATH="$PWD/.cache/review-tools/bun/package/bin:/usr/bin:/bin:/usr/local/bin"
export DIAGRAMDOCK_TEST_FIXTURE_PARENT="$(cat tmp/review/p2/fixture-parent.txt)"
export DIAGRAMDOCK_TEST_EXPECTED_FS=0x794c7630
export DIAGRAMDOCK_TEST_UNSUPPORTED_PARENT="$PWD/tmp"
export DIAGRAMDOCK_TEST_UNSUPPORTED_FS=0x6a656a63
export PLAYWRIGHT_BROWSERS_PATH="$PWD/.cache/playwright"
bun run test:e2e --grep 'committed save response' --output ../tmp/review/p2/baseline-playwright
bun run --cwd web lint && bun run --cwd web typecheck && bun run --cwd web test --coverage.enabled=false src/features/workspace/drafts.test.ts src/features/workspace/use-workspace.test.tsx
bun run build
bun run test:e2e --grep 'committed save response' --output ../tmp/review/p2/corrected-playwright
.cache/review-tools/bun/package/bin/bun tmp/review/p2/verify.ts
git diff --check
```

- Corrected results: frontend lint/types passed; **25 focused tests in two files passed**; source build passed with the existing greater-than-500-kB advisory; **all five built-source browser cases passed in 16.0 seconds**. Positive case made exactly three PUTs; every negative made exactly one. All five reported real revision/document observations, exact byte preservation and zero unexpected errors. BOM, CRLF, Unicode, surrounding prose and independent blocks matched the asserted original-file bytes. The overall corrected command status was 0. Logs and individual status files are under `tmp/review/p2/`; these are actual built-source service checks, not fresh executable checks.
- Inspected `tmp/review/p2/corrected-save.png`: the latest text is saved, preview is live and the false review warning is absent. Corrected origins were `http://127.0.0.1:43761` and `http://127.0.0.1:36217`; baseline origins were `http://127.0.0.1:33259` and `http://127.0.0.1:38907`. All were loopback-only and are now stopped. The independent verification script exited 0 after checking each stopped marker, unreachable listener, removed supported/refused roots and token/config files, then removed the empty exclusive fixture parent. Only the idle project tmux shell remains. Cleanup and structured case results are in `tmp/review/p2/verified.json`.
- Supplied implementation evidence, separately attributed: frozen installs plus complete `check:ci` passed at `4fa0938cd262ea151a9b14efb0b04d669eb28137`, including 54 frontend tests, 24 actual ARM64 executable browser cases, 93 embedded assets and four lazy families; reported binary SHA-256 `d781242be4fd39af05c5e5f94812c669482cbc28ae7922f7443165e87397e745`. Its empty-PATH/one-exec/no-checkout/no-extraction/scoped-write and cleanup results were supplied and recorded in UI-001, not re-executed by this review. No unrelated full gate, native raw research, new executable build or remote action was repeated here.
- **Delta verdict: PASS for the P2 correction and its session/cache regression scope. Overall verdict remains partial / in_progress:** P3 documentation drift is pending, native policy correction is separate, and corrected hosted source/Linux x64 executable/browser acceptance is still required. Run 34166805672 remains a failure at the old admission policy after the reported twenty passing raw ext4 controls. No new native or release success, design approval or final integration approval is recorded.

### Independent native policy assessment (2026-09-07)

- **Delta verdict: PASS for local policy review; `nativeAcceptance: pending`.** Reviewed `3cfe519605a15e43d937c5269794ccc4f76c3ba1` against `e70dd9c5779d8c1893c624f59480b9ab0832f316`; its code/config/tests match candidate `4ca471274d3ffa76469f8b2e87d4abd7055aa764`, with only this review record differing. No high-confidence introduced correctness, containment, privacy or resource-lifetime defect was found in the bounded change. This result does not establish native deployment readiness.
- `src/modules/diagrams/mount.ts:4-32` requires one valid fdinfo `mnt_id` and exactly one corresponding mountinfo record; it validates the selected record fields/separator and supported mount-point escapes, leaving unrelated options private. The helper only exports functions/types and has no CLI or startup side effects. The association and fields agree with [Linux proc documentation](https://docs.kernel.org/filesystems/proc.html); the four accepted mount-point escapes agree with [the kernel mountinfo formatter](https://raw.githubusercontent.com/torvalds/linux/master/fs/proc_namespace.c). `mountDevice`'s bounded 64-bit masks/shifts agree with [glibc major/minor decoding](https://raw.githubusercontent.com/bminor/glibc/master/bits/sysmacros.h), including high major/minor bits.
- `filesystem.ts:16-31` requires Linux, matching held/root device, matching encoded mount device, actual regular-file/directory metadata, and an exact name/magic pair: overlay/`0x794c7630` or ext4/`0xef53`. Ext2/ext3 names remain refused despite [sharing the ext magic](https://raw.githubusercontent.com/torvalds/linux/master/include/uapi/linux/magic.h); XFS, tmpfs, shared and unknown families remain refused. Missing, malformed or unassociated proc metadata yields writable=false and typed `filesystem_unsupported` on writes. Stat/open errors still propagate through the repository's existing safe error mapping. Neither environment values nor HTTP input change this allowlist.
- The three repository changes at `repository.ts:155,222,231` pass existing held FileHandles; their surrounding try/finally scopes keep them alive through the asynchronous statfs/fdinfo/mountinfo checks. Trusted script paths open their own O_NOFOLLOW directory handle and close it in finally. Existing root/path/inode/birthtime/device/hardlink/version validation, serialized saves, final revalidation and temporary-file cleanup are unchanged. The added metadata reads do not provide OS isolation or remove the accepted final comparison/rename window.
- Storage tooling uses the same parser and checks encoded device as well as ext4 name/magic. The API smoke now checks both its explicitly declared actual filesystem and production admission; changing a fixture expectation cannot grant write permission. Actual storage HTTP tests retain anonymous metadata absence, authenticated capability, byte-exact Markdown saving, independent in-place/atomic stale conflicts, traversal/symlink rejection, logout and real unsupported-root 503. Mount records/options and device metadata are not added to the authenticated session response.
- Independent environment: existing project-local Bun 1.4.2 and actual Node 24.20.0 on Linux ARM64, within the prescribed project tmux session. Verified a fresh exclusive canonical `/tmp` parent as overlayfs `0x794c7630`, device `70`, mount `318` / `0:70`; own ignored `tmp/` remained actual `0x6a656a63`, device `41`, mount `331` / `0:44` / fakeowner. The shared device discrepancy is preserved as observed evidence without a causal claim. Exact disposable identities are in `tmp/review/native/preparation.json`. No dependency/lock changes required another install.
- Ran the following scoped checks. `types.exit`, `tests.exit` and `checks.exit` were captured immediately as 0; inspected the actual log, not only status markers. Only this task was dirty during execution; the CLI refusal result records that fact and unchanged source commit accurately.

```bash
export PATH="$PWD/.cache/review-tools/bun/package/bin:/usr/bin:/bin:/usr/local/bin"
export DIAGRAMDOCK_TEST_FIXTURE_PARENT="$(cat tmp/review/native/fixture-parent.txt)"
export DIAGRAMDOCK_TEST_EXPECTED_FS=0x794c7630
export DIAGRAMDOCK_TEST_UNSUPPORTED_PARENT="$PWD/tmp"
export DIAGRAMDOCK_TEST_UNSUPPORTED_FS=0x6a656a63
bun run lint && bun run typecheck && bun node_modules/typescript/bin/tsc --noEmit --project tests/integration/storage/tsconfig.json
bun test ./src/modules/diagrams/filesystem.test.ts ./tests/integration/storage
.cache/review-tools/bun/package/bin/bun tmp/review/native/verify.ts
git diff --check
```

- Results: root lint/types and the strict storage project passed; **36 tests across three files passed, zero failed, 105 assertions**. This comprises 30 focused filesystem cases and six storage cases. Ext4 policy cases are synthetic; actual service operations used the separately identified local overlay/shared filesystems. The two injected missing/malformed-metadata saves were refused before temporary creation, with zero temporary writes, only the original file remaining and exact original bytes. The actual unsupported CLI control intentionally exited 1 with `stages: []`, unchanged sentinel bytes/directory contents, `nativeSourceChecks: failed` and `nativeAcceptance: pending`; its successful test does not relabel that refusal as native acceptance.
- Independently checked all six emitted fixture removals and removed the empty exclusive parent. The verifier exited 0; results are in `tmp/review/native/verified.json` and `checks.log`. HTTP checks were in-process; this delta started no listening service or browser and created no external runtime/token/config files. Only the idle project tmux shell remains. No new preview, binary, full aggregate gate or local native raw probe was run.
- **Historical remote evidence now inspected directly:** read the authorized provenance, storage result/admission, failed-job log and all twenty raw JSON/exit records for [run 34166805672, attempt 1](https://github.com/itxje/diagramdock/actions/runs/34166805672). Every child has exit 0, no signal, 150 events, succeeded=true, boundarySnapshots=false, zero temporary leftovers and reported fixture removal on ext4/`0xef53`. Provenance identifies clean `c1078e302cfe43eeaa8fc5bf9286a09273a74ee5`, Linux x64, Ubuntu image `20260831.293.1`, Bun 1.4.2 and actual Node 24.20.0; native device/mount `2049` / `27` / `8:1`, refusal tmpfs device/mount `26` / `32` / `0:26`. Admission remained writable=false under the old overlay-only policy, with nativeSourceChecks=failed, native/browser/binary pending and cleanup reported true. The job log confirms exit 1 at that admission failure. Per-record digests and a bounded summary are retained only in ignored `tmp/review/native/history-summary.json`; no full historical record was copied into repository documentation or re-executed. This supplements the earlier metadata-only inspection without changing the historical failure interpretation.
- Supplied combined candidate evidence remains separate from this independent review: at clean `4ca471274d3ffa76469f8b2e87d4abd7055aa764`, frozen installs/full `check:ci`/whitespace passed with 115 file, six storage, one procfs, 152 backend, 54 frontend and 18 release tests, 24 ARM64 executable browser cases, 93 assets and four lazy families. Counts overlap. Reported binary SHA-256 `bdc7f779e3d13330217a82a3213417ccc67c7aaeee640c4e5489a8663f5a291e`, 85,444,904 bytes; supplied traces show one exec each, 528/8 paired calls and 16/0 scoped writes for supported/refused roots, empty PATH, no checkout/extraction, zero unexpected browser errors/external requests and verified cleanup. These are supplied local ARM64 results, not an independently executed native run here.
- P2 stays resolved and P3 documentation reconciliation remains pending. The actual corrected hosted ext4/Linux x64 source plus copy-only executable, all 24 browser cases, assets, traces and cleanup must still be reviewed at the exact candidate commit before native acceptance. At completion of this finite policy audit, no new remote result had been supplied; the later metadata update follows below. Prototype needs-review, practical save semantics, retained `applicationSafetyPassed=false` and final integration approval boundaries remain unchanged.

### Successful hosted metadata follow-up (2026-09-07)

- Continued the existing claim from clean tracking commit `88f7a107e5b922a30d91941d398c6c0c85400f99`, with application source unchanged. This evidence-only follow-up verifies public run/job/artifact metadata and updates the remaining review boundary; it does not repeat code gates or infer detailed acceptance from workflow status.
- Independently verified [run 34168525432, attempt 1](https://github.com/itxje/diagramdock/actions/runs/34168525432) through three read-only public APIs: exact candidate `4ca471274d3ffa76469f8b2e87d4abd7055aa764`, push event, completed **success**. The verify job, native source/executable acceptance step and checked-executable retention step succeeded; publication was skipped. Artifact `10034988497`, `checked-linux-x64-4ca471274d3ffa76469f8b2e87d4abd7055aa764`, exists, is not expired and is associated with the same source SHA.
- `.cache/review-tools/bun/package/bin/bun tmp/review/native-success/metadata.ts` exited 0 after checking HTTP 200 and the exact identities/statuses from all three endpoints. Local metadata and the bounded summary are in `tmp/review/native-success/`. At this metadata-only stage, no archive contents, detailed test results, filesystem identity, binary digest, traces or cleanup records from the successful run were inspected; none were inferred from its green status or artifact name.
- The old run 34166805672 remains a retained admission failure. At this stage, detailed evidence from the corrected run was still required before accepting native execution. The subsequent log assessment below advances that boundary. P2 stays resolved, documentation reconciliation remains pending and prototype status stays needs-review; no release, tag or final integration approval is recorded.

### Successful hosted execution log assessment (2026-09-07)

- Continued the existing in-progress claim from clean tracking commit `f147fca3664d2d1ff8893e7e312d35528d35f7c7`, without changing application source. Investigation: the successful run's actual log and its supplied structured summary are now available for bounded read-only inspection. Proposal under the existing evidence-review authorization: verify the log digest, independently parse its unique source/smoke/package records and test summaries, compare their identities/results, and update acceptance without repeating CI or claiming an independent artifact download.
- Independently read the actual log for [run 34168525432, attempt 1](https://github.com/itxje/diagramdock/actions/runs/34168525432) and verified SHA-256 `4e83324b6d688bcf1490187a5cf702f71d06ac4f5d69137d83887516ee2445d0`. Parsed records identify clean candidate `4ca471274d3ffa76469f8b2e87d4abd7055aa764`, Bun 1.4.2, Linux x64, actual ext4/`0xef53` device `2049`, mount `27` / `8:1`, and refusal tmpfs/`0x1021994` device `26`, mount `32` / `0:26`.
- Source stage records contain exactly twenty ordered raw stages followed by `files` and `http`: all 22 exits are 0 with null signals; `nativeSourceChecks=passed`, failure=null and cleanup=true. The source-stage `nativeAcceptance=pending` correctly precedes the executable stage. The later smoke record reports `result=passed`, `nativeAcceptance=passed` and cleanup=true on matching actual filesystem identities. Historical `applicationSafetyPassed=false` is retained.
- Actual log summaries show 152 backend tests with zero failures, 54 frontend tests, 18 release tests with zero failures, and **24 executable browser cases passed in 1.2 minutes**. The final aggregate reports `check:ci` passed for `bun-linux-x64`. The smoke verifies **all 93 embedded assets**, flowchart/sequence/class/state families, zero unexpected browser errors and zero external requests.
- Emitted trace audits report refusal: one exec, 3 paired calls, 404 file accesses, zero writes; supported: one exec, 251 paired calls, 19,833 accesses, 16 scoped writes. Both report zero checkout accesses, empty runtime PATH and no frontend extraction. These are inspected audit summaries, not an independent replay of hosted raw traces. The log reports cleanup=true; hosted test-only origins `http://127.0.0.1:38641` and `http://127.0.0.1:37589` are not a public or currently live preview.
- The unique real 93-asset package record reports `diagramdock-0.0.0-ci.fixture-linux-x64`, raw executable with no resource archive, SHA-256 `9be56bbd7551bdd27806571a133112947aee2deb27b6dbdce3f030c71337f354`. Earlier two-asset mocked package hashes are excluded. This is a reported package digest; the checked executable and manifest have not been independently downloaded or hashed in this review.
- `.cache/review-tools/bun/package/bin/bun tmp/review/native-log/verify.ts` exited 0 after digest, provenance, ordered-stage, filesystem, count, audit and package assertions, also matching the supplied structured summary. Its initial attempt exited 1 because the scratch parser did not normalize literal caret-form ANSI codes in the frontend summary; that formatting-only parser correction was retained separately and the corrected check passed. No application test or CI job was rerun. Bounded evidence and the actual exit status remain in ignored `tmp/review/native-log/`; `git diff --check` passed.
- **Execution evidence accepted within the log's scope:** corrected Linux x64/ext4 source and executable acceptance were observed, with publication skipped. Successful raw child JSON and raw syscall traces are absent from this log and were not personally re-read. At this stage, the executable/manifest digest handoff and P3 documentation reconciliation remained pending; the subsequent artifact assessment follows below. The original failed run, practical final comparison/rename window, local OS actor limitation and needs-review prototype status remain unchanged.

### Downloaded native artifact assessment (2026-09-07)

- **Native evidence verdict: PASS at exact candidate `4ca471274d3ffa76469f8b2e87d4abd7055aa764`.** Independently inspected the supplied downloaded package and bounded run records for [successful run 34168525432, attempt 1](https://github.com/itxje/diagramdock/actions/runs/34168525432), artifact `10034988497`. Run/artifact/source identities match the previously reviewed execution; publication remains skipped. This closes the native source/Linux x64 executable evidence gate without changing source or repeating execution.
- The actual executable `diagramdock-0.0.0-ci.fixture-linux-x64` is **85,485,024 bytes**. Independently computed SHA-256 **`9be56bbd7551bdd27806571a133112947aee2deb27b6dbdce3f030c71337f354`**, matching the exact SHA256SUMS line and previously inspected real 93-asset package log. The current `releaseFiles` strict validator passed: exactly executable/checksum/internal manifest, bounded regular non-symlink entries, valid ELF64 little-endian x64 header and consistent manifest tag/version/filename/commit. Manifest values are `v0.0.0-ci.fixture`, `0.0.0-ci.fixture`, `bun-linux-x64`, Bun `1.4.2`, the exact candidate SHA and 93 unique valid assets.
- Artifact metadata separately reports a transport archive of 85,506,772 bytes with digest `dc0b9dd49f04f5bdb86fb8156ac4d9252627378eff9f7ba2c059d3339f402b5e`. That archive was not re-downloaded or independently hashed here; its identity is distinct from the independently hashed executable. The fixture tag/version describes verification metadata, not an actual selected release or created tag.
- Read-only `readelf` inspection confirms x64 and interpreter `/lib64/ld-linux-x86-64.so.2`, with libc, the system loader, pthread, dl and math shared libraries. Runtime/backend/frontend embedding does not remove these ordinary Linux system requirements or provide a static/all-OS executable. The supplied binary was never executed on this ARM64 review host.
- The newly supplied log matches the previously verified log SHA-256 exactly. Supplied structured source and smoke records match the previously independently parsed records: actual ext4/`0xef53` device/mount `2049`/`27`, expected tmpfs refusal `26`/`32`, twenty raw stage exits plus file/HTTP exits all 0, then complete executable `nativeAcceptance=passed` and cleanup=true. The prior 24-browser/93-asset/four-family and emitted trace results remain applicable to these exact executable bytes. Source-stage pending status and the old run 34166805672 failure remain historical facts; successful raw child JSON and raw syscall traces were not uploaded or independently replayed.
- `bash tmp/review/native-artifact/run.sh` ran once in the existing project tmux session with pinned local Bun. Its verifier and readelf inspection exited 0; actual result/log/status and ELF evidence are in ignored `tmp/review/native-artifact/`. `git diff --check` passed. No download, server, browser, project fixture, credential/config file, binary execution or aggregate test run was started; the verification window exited, leaving only the idle project shell. Existing hosted loopback test origins remain historical test evidence, not live previews.
- P2 and local policy review remain passed; no new finding was introduced. **Only P3/final documentation reconciliation remains for this review.** Overall status stays partial / in_progress until that accepted documentation delta is reviewed. Prototype needs-review, practical final-write-window and OS-actor limits, retained universal-loss diagnostic, and final integration approval remain unchanged; no tag or release was created.

### Final documentation assessment (2026-09-07)

- **Final implementation review: PASS; no remaining actionable findings.** Reviewed accepted documentation from `409df49` through `7d6d720a37123d200463ac6473fdafe48964d8c2`, including README, AGENTS, environment comments, SECURITY, architecture, changelog, delivery notes and DOC-001 evidence. P3 is resolved. P2, native policy, actual native execution and downloaded-artifact validation remain closed at their recorded revisions.
- Confirmed the authenticated editor/SPA routes and implemented embedding replace the stale starter/health-only/root-404 claims. Clone access, project-local Bun 1.4.2, actual Node 24.20.0, independent frozen installs, private token input, original-root configuration, nsl run/discovery, source production startup and single-executable commands agree with current manifests/scripts/config. The complete gate includes its browser/actionlint/tracing prerequisites. System loader/glibc requirements, runtime-only root/auth inputs and local-only preview scope are explicit.
- Current native claims identify exact candidate `4ca471274d3ffa76469f8b2e87d4abd7055aa764`, successful run 34168525432 and independently verified executable SHA-256 `9be56bbd7551bdd27806571a133112947aee2deb27b6dbdce3f030c71337f354`. Dated pending records, failed run 34166805672, source-stage pending chronology and missing successful raw probe/trace limits remain intact. Direct external edits, typed conflicts/deletion, retained dirty drafts, unsupported writes, final comparison/rename and local OS actor limits are accurately distinguished. No first public version/tag/release, static/all-OS binary or approved prototype is claimed.
- Ran `bash tmp/review/final-docs/run.sh` in the prescribed project tmux session with pinned Bun; the owned TypeScript checker exited 0. Independently compared committed trees/modes against the accepted native candidate: **195 other tracked entries preserved**, including application, scripts, tests, manifests/locks, workflows, designs, examples, untouched documentation and the existing symlink. All **15 configuration assignments** are unchanged and correspond to validated keys; environment changes are comments only. The prior review record was preserved by integration. Checked **nine Markdown documents, 73 relative links/anchors and 22 Bash fences** with Markdown parsing and `bash -n`; all passed. `git diff --check` passed. Final result/log/status are in ignored `tmp/review/final-docs/`.
- Independently inspected the specifically supplied setup, HTTP, browser and prototype logs plus their immediate zero exit records. They support fresh pinned/frozen setup/build, actual HTTP save/409/logout checks, four built-source browser cases in 21.6 seconds with zero unexpected errors, and nine prototype verification groups. Supplied lifecycle records preserve overlay device 70 sample bytes, stopped/removed refusal fixtures and the two explicitly retained previews with a private mode-0600 token. These are inspected prior execution results, not fresh browser or executable runs in this phase. Personally viewed the supplied desktop application, 390-pixel preview and desktop prototype screenshots: file tree/editor/canvas hierarchy and narrow tabs agree with the documented reference; the narrow capture is dark theme. Prototype status remains needs-review.
- Fresh read-only check `.cache/review-tools/bun/package/bin/bun tmp/review/final-docs/http.ts` exited 0 at 23:21 UTC. Source `http://127.0.0.1:42927` returned HTTP 200 for `/`, `/api/health` and `/api/session`; the session was exactly unauthenticated and no cookie was set. The served prototype at `http://diagramdock-design-4984b3.localhost:3003/diagramdock/DiagramDock.html`, explicitly resolved to loopback, matched the accepted HTML bytes. This verifies local-machine HTTP access only, without credentials or a fresh browser session. No server, browser, fixture or token was created or removed; the existing previews remain intentionally available and their documented owner controls cleanup.
- Completed only REVIEW-001 and its own index marker. Documentation/plan status-only completion belongs to their existing owner and does not require repeating this review. No application gate, native probe, binary/browser execution, artifact download, remote write, main integration or publication was repeated. The implementation review passes within the recorded evidence and known limitations; it does not approve final main integration, publication or the assumed prototype design.

### Boundary assessment and retained limitations

- File boundary: inspected validated relative paths, descriptor-anchored O_NOFOLLOW traversal, root/device/identity/procfs checks, hardlink refusal, bounded UTF-8 reads, full-file revisions, serialized same-directory replacement and cleanup. Inspected tests for symlink/root/ancestor substitution, independently performed external changes, stale conflicts, typed deletion, consecutive standalone/Markdown saves and preservation of unrelated bytes. No additional high-confidence HTTP containment or data-integrity defect was confirmed in this scope.
- Practical saving remains optimistic. An uncooperative external writer can be overwritten in the final comparison/rename window; retained `applicationSafetyPassed=false` is a successfully reproduced universal-loss diagnostic, never a passing no-loss regression. The explicitly selected practical guarantee does not claim OS isolation when a local actor moves roots/ancestors. Detected conflicts/deletions must still fail; this review does not waive them or authorize force overwrite.
- HTTP/render boundary: inspected token/session limits, Host/Origin/CSRF checks, cookies, secret-safe errors, bounded bodies/timeouts, exact static maps and CSP. Reviewed strict serialized Mermaid rendering, input restrictions, SVG sanitization, sandboxed preview, stale-result handling and settled malicious-source browser assertions. No additional high-confidence auth bypass or rendering escape was confirmed. CLI-sourced base-nova/Base UI, Tailwind, file routes and Query are present without a forbidden UI ecosystem or database/workspace/cloud runtime dependency.
- Automation: inspected default read permissions, tag-only publication write permission, absence of privileged PR secret flows, frozen locks, compile/asset failure cleanup, strict tag/version/commit/inventory/checksum validation and nondestructive draft/rerun mismatch refusal. Publication transport regressions are local tests; the initial local audit had no supplied hosted result. The subsequent hosted failure and corrected successful candidate metadata/execution log are recorded above, both with publication skipped; no real release publication has been verified. Verified action pins against official repository data; checked executable and workflow semantics against [executable documentation](https://bun.sh/docs/bundler/executables) and [workflow security guidance](https://docs.github.com/en/actions/reference/security/secure-use).
- Evidence boundary: the reviewed candidate admits only the exact Linux overlay or ext4 name/magic pairs with matching held/root/encoded mount devices and valid descriptor metadata. Local policy review passed; actual shared/tmpfs refusal remains evidenced. The historical hosted failure and twenty raw ext4 records remain intact. Corrected run 34168525432 has independently checked metadata, actual native source/executable/browser execution logs and downloaded executable/manifest/checksum agreement at the exact candidate. Successful-run raw child JSON and syscall traces were not available for independent replay; the inspected emitted audit summaries retain that limit. Native evidence and final documentation reconciliation are accepted within these scopes.
- P2, P3, local native policy, corrected native/x64 execution and exact checked-artifact validation are resolved. No further implementation review is required solely for the documentation owner's completion markers. Substantive later changes require their affected checks/review. Real publication and final integration approval remain separate from this implementation review.

| Severity | Count | Status |
| --- | --- | --- |
| CRITICAL / P0 | 0 | None confirmed |
| HIGH / P1 | 0 | None confirmed |
| MEDIUM / P2 | 1 | Resolved at `e70dd9c`; no open P2 finding |
| LOW / P3 | 1 | Resolved at `7d6d720`; no open P3 finding |

**Verdict: PASS / completed.** P2 and P3 are resolved, with no remaining actionable findings. The initial complete local gate, focused correction/policy/documentation checks, actual hosted native/Linux x64 execution and independent checked-artifact validation passed within their recorded scopes. Practical save/OS-actor limitations and unavailable raw hosted evidence remain explicit. Only this review task is completed; no overall plan completion, final main integration, release or design approval is recorded.

### Original checklist and acceptance amendments

- Claim before investigation, re-read index/detail, and persist review investigation/proposal here. Read the full changed code, relevant tests and current task evidence before making findings.
- Read `/pma-cr` and its relevant TypeScript backend/frontend review packs. Keep findings and resolutions in these Notes; do not create extra report files.
- Inspect filesystem containment/races, atomic save failures and conflicts, Markdown offsets, watcher lifecycle, credentials/redaction/Origin/Host, strict rendering and outbound resources, dirty draft behavior, component sourcing and accessibility.
- Verify no database, workspace, forbidden UI packages, unrequested public licensing or remote service dependency entered the implementation. Confirm prototype status accurately reflects review evidence.
- Original planned checks were superseded by the complete frozen-install and `check:ci` command recorded above, which includes the browser gate. Repeat checks only as changed code or unresolved findings require. Inspect the actual diff and lockfiles; do not infer correctness solely from green tests.
- Record severity, concrete path/reference, resolution and residual limitations. The local audit is recorded above with remaining acceptance explicitly pending. This task does not authorize final main integration.

- 2026-09-07 acceptance clarification: consume SAVE-001 only after acceptance/integration. Keep the downstream chain sequential. Exercise actual API/UI/browser/binary file saves on the explicitly configured, verified Linux overlayfs root described in README/PLAN-004; test real unsupported-root refusal separately. Preserve external direct edits and dirty drafts, expose filesystem_unsupported without force-save or automatic retry, and do not relabel the retained after-final-validation diagnostic as passing application safety. PLAN-001's polling contract governs refresh; older watcher/event wording above does not add a streaming subsystem.

- Delivery dependency clarification: TEST-001 is followed by NATIVE-001, then CI-001. Review must distinguish local overlay browser evidence from actual ordinary Linux/native and standalone executable evidence; native acceptance may remain pending until the real hosted run. Test fixture type overrides never grant production write support. Inspect TEST-001's exact error/resource audit, coverage boundaries, fixture cleanup and remaining gaps.
- Native evidence contract: consume PLAN-005's versioned runner results and exact remote commit/run/filesystem plus deployed executable browser evidence. Green preparation can advance independently; neither preparation nor a refused target completes native acceptance. This note does not change this task's owner or status.

- CI/release acceptance (authorized 2026-09-07): inspect PLAN-002, immutable action pins, least privilege, strict tag-derived versions, draft/rerun safety, compile failure cleanup, checksums and complete embedded-asset inventory. Require actual binary-only traced runtime independence and authenticated browser save/conflict/byte preservation on the documented filesystem. Downloaded executables require no Bun/Node/source/frontend directory. Native Linux x64 and actual hosted run/publication status are separate evidence gates; preparation or local workflow validation cannot establish them. First release version remains unchosen and prototype needs-review.
