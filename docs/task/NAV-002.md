# NAV-002 Preserve linked flowchart layout and titled file navigation

- **status**: completed
- **priority**: P1
- **owner**: Frontend maintainer
- **createdAt**: 2026-09-12

## Description

Repair flowchart node positioning and safe internal file navigation for supported relative click statements, including title-only front matter. Verify real browser geometry for grouped and ungrouped linked nodes, pointer and keyboard navigation, unchanged source bytes and draft retention, and the existing security and UI contracts.

## ActiveForm

Preserving linked node layout and file navigation.

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

- Claimed on 2026-09-12 in the isolated issue worktree. The checkout is clean at the requested base before task creation.
- Authorization: the project owner explicitly requested investigation, proposal, implementation, regression verification and incremental self-review as one source repair. On 2026-09-12, after the aggregate failure was explained, the owner additionally authorized committing and merging the repair into local `main`. Publication and deployment remain outside this task.
- Investigation and proposal: recorded before implementation in [PLAN-023](../plan/PLAN-023.md). Anchor stripping loses Mermaid's wrapper transform, and target extraction omits supported front matter. Implement the validated render projection and title-aware internal navigation within the existing authorization.
- Exit criteria: source fix, meaningful browser RED/GREEN evidence, standard quality gate and incremental self-review, with the source candidate ready for review.
- Implementation: original-source validation precedes a private render projection that blanks only accepted file click statements. Validation, target extraction and projection share quote/comment boundaries. Target extraction handles the supported title prefix, and stale preview metadata is cleared until the corresponding source settles. Both sanitizer policies, file containment, limits, label editing and navigation/draft storage remain intact.
- RED evidence: `tmp/nav-002/red-policy.log` has two failed title extraction cases (LF and CRLF). `tmp/nav-002/red-browser.log` and `red-browser-results/` show both generic topology variants failing with 27 overlapping node pairs on the baseline build.
- Construction evidence: retained `green-lint-red.log` records corrected import/bracing formatting. All 200 focused preview tests passed in `green-filtered-coverage.log`; that filtered invocation failed the repository-wide coverage threshold, so verification continued with the full frontend suite, which passed 258 tests and its unchanged coverage thresholds in `green-first.log`.
- Assertion corrections: nested file headings display the full project-relative path, so the new navigation expectations now assert that path, matching the route and document checks. `geometry-diagnostic.log` and `geometry-diagnostic-results/` retain the titled layout comparison failure: identical title markup produced viewBox origins of -56.70 and -55.35 on successive browser measurements, moving all screen coordinates together. The comparison now uses ROOT-relative positions at the same scale with the original 0.05-pixel precision; positive visible bounds, intended-group containment and zero overlaps remain mandatory for both versions. No product or security requirement was removed.
- Supplied-input evidence: `tmp/nav-002/original-result.json`, `original-geometry.json`, `original-fixed.svg` and `original-fixed.png` record the unchanged input served by the isolated built application: 18 positioned nodes, four containing groups, seven edges, 17 targets, zero node overlaps, pointer/Enter/Space navigation, exact API/editor/file source, zero implicit writes and no browser errors. The service was stopped and its disposable root removed.
- Focused GREEN: both geometry variants pass in `tmp/nav-002/geometry-green.log`, and both title variants of nested/Markdown navigation, Tab/Enter/Space, missing-file refusal, stale-link clearing, exact explicit saves and two retained drafts pass in `affected.log`. The earlier affected run also passed label editing, pan/zoom/panes, styled flowcharts, contrast, label breaks and Gantt coverage.
- Standard gate limitation: both frozen installs and workflow lint passed, but the normal `check:ci` stopped at the unchanged `read-consistency.test.ts` test "continued read churn stops after exactly three complete attempts with conflict" (154 passed, one failed). An instrumented local probe (`churn-ext4.json`) confirmed equal-size successive writes retained identical mtime/ctime values and the read returned the current bytes after two attempts; a separately identified overlay parent reproduced the same original test failure (`churn-overlay.log`). The backend and its fixtures are byte-identical to the inspected base. This is an existing read-consistency validation failure in this environment, not a frontend regression or a passing gate. No file safety behavior or acceptance assertion was changed to hide it.
- Remaining standard checks were run separately with their original commands/functions (`tmp/nav-002/remaining-results.json`). Storage type checking and six storage tests passed. Root lint/types and focused acceptance proceeded successfully; the full backend coverage run had 202 passes and the same one read-churn failure. Frontend lint/types and all 258 tests passed with 94.51% line and 89.39% branch coverage. All 24 release tests passed. Local executable and bundle smoke each passed 48 browser tests, with the normal existing-service skip of one open-access case; that case passed separately in `open-access-check.log`. All 49 browser cases therefore have passing evidence. Both local artifacts passed asset/lazy-diagram checks; executable tracing verified 94 embedded assets, a single executable, empty runtime PATH, no checkout access or frontend extraction, and complete service/root cleanup. These component results do not turn the failed aggregate into a pass.
- Tooling and scope: Bun 1.4.2 / Node 24.20.0, Playwright 1.63.0, identified ext4 support and tmpfs refusal storage, isolated browser libraries/fonts and the real strace executable. Local fixture artifacts were built from the reviewed code tree before commit; `verified-code-files.json` records those code bytes, and their build metadata records the inspected base. The normal native delivery gate was not run; local leaf smoke output does not establish a passing aggregate or delivery approval.
- Incremental self-review: inspected the changed production and regression code under the frontend review policy, both sanitizer boundaries, the unchanged relative-path validator and exact-tree internal navigation, draft lifetime, event containment and stale-render cancellation. No actionable finding was introduced by this diff. The baseline read-consistency gate failure remains an integration limitation for review.
- Outcome: source implementation, focused regression verification and incremental self-review are complete; the candidate is ready for review with the explicitly failing baseline gate and the subsequent authorization for local integration. No publication, deployment or installed-bundle change is included.
