# CI-001 Verify continuous integration and executable releases

- **status**: completed
- **priority**: P1
- **owner**: Automation maintainer
- **createdAt**: 2026-09-07

## Description

Implement reproducible checks and tag-triggered, least-privilege release automation for a single executable containing the runtime, backend and complete built frontend. Keep ordinary Linux acceptance and actual hosted results explicit.

## ActiveForm

Verifying continuous integration and executable releases

## Dependencies

- **blocked by**: NATIVE-001 (preparation completed after TEST-001)
- **blocks**: REVIEW-001, then DOC-001

## Notes

Reclaimed by Automation maintainer for the authorized bounded trace-parser correction before investigation. The prior local completion evidence remains historical; the independent integration check failed after browser success because an interleaved unfinished openat record was audited before its matching resumed return. This is not a passing integration. Scope is limited to the trace audit, its regressions and these tracking notes; existing authorization covers the correction.

Claimed before substantive investigation as Automation maintainer. Related plan: PLAN-002. Existing user authorization on 2026-09-07 covers the original MVP, exact repository git@github.com:itxje/diagramdock.git, CI/version-tag releases, the single-executable clarification and direct external editing. No later approval event is inferred. No first version is selected. Prototype remains needs-review; final integration remains a separate approval boundary.

Read PLAN-005 before implementing: native preparation is complete but nativeAcceptance=pending. The first real ext4 run must retain twenty raw results and visibly fail the current write-admission policy. Local overlay or cross-compilation cannot establish native or Linux x64 runtime acceptance. Production file admission is outside this task.

## Acceptance

- Pinned runtimes and immutable verified action references; frozen sibling installs; real source, browser, storage and workflow checks.
- A nonpublishing candidate check collects actual hosted native provenance and failures before main integration.
- A tag-derived versioned executable embeds all frontend assets without runtime extraction or installed JavaScript runtimes. Binary-only tracing, all-asset verification, real authenticated browser saves/conflicts and cleanup run outside the checkout.
- Release checks cover strict versions, checksums, compilation failure cleanup and safe draft publication/reruns with a local mock transport. Publication is gated on actual matching-runner native checks.
- Record exact local evidence and remaining remote/native/x64 gaps; no push, tags, release creation or final integration performed by this task.

## Investigation and implementation record

PLAN-002 was recorded before implementation under the original 2026-09-07 authorization. The existing source/bootstrap already accepted an injected static map, so no file/auth/editor or static routing change was necessary. Added only shared startup/build metadata for compiled entry injection. The complete Vite output is validated, snapshotted into unique ignored compile inputs and embedded with file import attributes. There are no tracked stub swaps, frontend extraction, operator-root files or credentials in the executable. Source startup remains available.

Implemented one workflow for PR/main/candidate/manual checks and tag-only publication, verified action SHA pins and actionlint setup, meaningful aggregate gates, a real native runner handoff, raw executable/checksum packaging, binary-only tracing/browser checks and safe draft/rerun API publication. Root @playwright/test 1.63.0 was verified at the official registry and added only for typed executable verification; the independent frontend manifest/lock and all application versions are unchanged. A narrow HTTP test expectation now compares the observed expected filesystem and actual service policy label while retaining writable/auth/redaction assertions. No write-admission expansion was made.

## Actual local verification

The complete required command ran in the owned persistent project tmux session at clean implementation commit `b4f84a22423ec0b532542407c3d2d74da26d95cd`:

`bun install --frozen-lockfile && bun install --cwd web --frozen-lockfile && bun run check:ci && git diff --check`

Actual environment: integrity-verified project-local Bun 1.4.2 (744846f84), separately executed Node v24.20.0, Linux arm64, strace 6.13 extracted into project .cache from the distribution package, and project-local Playwright 1.63.0 / Chromium 153.0.8010.12. No system runtime, mount, credentials or globally installed tool was changed. Explicit positive fixture parent was an exclusively created disposable directory recorded in `tmp/ci-cleanup.json` (overlayfs 0x794c7630, device 70); refusal parent was this checkout's `tmp` (host-shared 0x6a656a63, device 41). These are exact observations, not native filesystem claims. Both parents were explicitly selected; no operator project was relocated.

| Stage | Observed result |
| --- | --- |
| Frozen root/web installs | Both exit 0; existing application versions/web lock preserved |
| Workflow validation | actionlint 1.7.12, one workflow passed; all six action references match verified full release commits/comments; tool archive digest verified before installation |
| File gate | Strict tsc, 85 tests / 0 failures / 664 assertions; separate real domain smoke exit 0, including two selected blocks and external refresh |
| Storage preparation | Strict tsc, 6 tests / 0 failures / 54 assertions; actual refusal control stays nativeSourceChecks=failed, nativeAcceptance=pending, stages empty, cleanup=true |
| Source check | Lint and strict types passed; isolated missing-procfs 1/0; backend 122/0, 99.57% functions / 99.44% lines; frontend 43/0 across 9 files, 91.97% statements / 88.66% branches / 88.79% functions / 92.68% lines; complete production build passed |
| CI/release logic | Both focused strict projects and 11 tests / 0 failures / 69 assertions; strict versions/metadata/checksums/ELF/inventory, real bundler failure, interrupted generated-input preparation, source/scratch preservation, secret redaction across pipe chunks, trace controls and nonpublishing draft/rerun safety |
| Actual executable | bun-linux-arm64, 85,444,904 bytes; version 0.0.0-ci.fixture, commit above, embedded Bun 1.4.2; --version and --build-info passed with PATH empty and no root/token |
| Complete assets | 93/93 exact HTTP body hashes/sizes/MIME/security headers: 91 JavaScript chunks, 1 stylesheet, 1 HTML shell. This Vite build emits no font/image files; any future emitted allowed resources enter the same full inventory |
| Real browser | Existing 19 cases passed in 47.0 seconds against the executable; zero unexpected console/page/network errors. Additional real flowchart, sequence, class and state lazy rendering passed, zero external requests |
| Traced runtime proof | Each service trace shows exactly one execve of the copied executable, no child runtime and no source/frontend dependency. Supported trace: 13,137 file-access records / 9 scoped save opens; refusal trace: 381 / 0 writes. Empty PATH, no frontend extraction; actual runtime directory contained only the executable |
| Cleanup | Both final services stopped with exit 0, URLs no longer reachable, all generated roots/runtime directories/credentials and compile scratch absent; empty owned positive parent removed independently |
| Final whitespace/status | git diff --check exit 0; implementation commit was clean during the full gate |

The 85 focused file tests overlap the 122 backend tests and must not be added as unique counts. Native storage preparation tests are not native raw or admission success. Existing frontend coverage exclusions and Vite's >500 kB chunk advisory remain unchanged. No performance or complete Mermaid-syntax support is asserted.

Final ARM64 raw artifact: `dist/release/diagramdock-0.0.0-ci.fixture-linux-arm64`. SHA-256: `67236d7227761ade8c5a6b9d9a66836e1d9955d0e1232e7d630d066d279cc360`. Internal checked inventory contains exactly that executable, SHA256SUMS and manifest.json. Public release assets would contain only the executable and SHA256SUMS; no transport/resource archive is produced. No real version tag exists for this fixture.

Actual final smoke origins were `http://127.0.0.1:45675` (supported) and `http://127.0.0.1:44583` (refused). They were loopback-only and are now stopped. The executable ran alone in an exclusive outside-checkout runtime directory with separate synthetic project/auth inputs. Assertions include anonymous file denial, unknown API/static/source/env rejection, consecutive standalone saves, byte-preserving independent Markdown blocks, stale/external in-place and atomic conflicts, deletion/rename, retained dirty drafts/in-flight saves, unsupported-root refusal, session recovery and strict rendering. Root/ancestor/procfs controls remain covered by the real file/source gates; no OS isolation claim is added.

Evidence: `tmp/ci-final.log`, `tmp/ci-final.exit` (0), `tmp/release-smoke-fe3NdX/result.json`, both raw local .trace files, `tmp/e2e-uJmzEM`, `tmp/ci-cleanup.json`, `tmp/actionlint-setup.log`, and `tmp/ci-evidence` bounded redacted export. Screenshots include `tmp/release-smoke-fe3NdX/lazy-diagrams.png` (1440x920, inspected), and the existing browser suite's desktop/dark/narrow/conflict screenshots. No live preview remains and no public reachability is claimed.

## First failure and focused corrections

The initial complete command reached all 19 binary browser passes (51.3 seconds), all 93 resource matches and all four lazy families, then failed the trace audit. strace -yy includes the current working directory in AT_FDCWD annotations even for an absolute /proc/self/fd open. A whole-line substring test mistook this cwd annotation for resource extraction. The actual returned descriptors pointed to scoped synthetic atomic-save temporaries. Corrected the audit to validate the returned descriptor target and added a regression preserving rejection of actual extraction. Both service exits were already 0; the original failed result/trace remains in `tmp/release-smoke-mnKrtq` and `tmp/ci-first.log`. Independent corrected trace review and cleanup are recorded separately rather than rewriting that failure.

Initial focused lint found script formatting/capture/type-import issues; they were fixed before the first complete gate. Scoped review additionally required typed root browser tooling rather than importing web/node_modules by path, bounded regular-file checks before manifest/evidence reads, explicit post-cleanup absence checks and cross-chunk token redaction for failed browser output. Corresponding focused checks and the final complete command passed. No production source defect or new file-admission policy was introduced.

## Scoped implementation review

Applied the shared review policy and TypeScript backend/frontend boundary packs to the full diff and surrounding startup/static/auth/render/file contracts. Confirmed exact root/auth/CSRF/Host/Origin/health/static headers remain, all assets use trusted compiled map keys, secrets are never build inputs, generation does not rewrite source, compile errors retain previous complete artifacts, output inventory/version/hash/ELF and matching-runner checks fail closed, and tests cannot override storage admission. Reviewed least privilege, immutable actions, candidate event semantics, native failure preservation, artifact provenance, API host/input limits, bounded authenticated draft listing, complete upload-before-publication and mismatch refusal without deletion. Browser resource audits and source coverage remain meaningful; prototype metadata and visual implementation are unchanged.

Resolved introduced findings: trace cwd/target confusion, test-log token redaction and pre-read artifact type/size checks. Remaining actionable introduced findings: Critical 0, High 0, Medium 0, Low 0. Verdict: PASS for implementation preparation and verified local ARM64/overlay execution. This is not final release or ordinary Linux readiness.

## Remaining delivery gates and handoff

Native ext4 source acceptance, Linux x64 execution, actual hosted workflow runs and remote publication remain unrun/pending. The current production policy still refuses ext4. First hosted `verify/native-readiness` execution must record the reviewed candidate's exact commit/run/runner/native type/device, preserve exactly twenty raw observations, and visibly fail admission. That evidence can support the separately bounded storage correction; only subsequent real native source plus single-executable/browser success permits publication. No initial version was chosen, no branch/tag was pushed and no release or main integration was performed. PLAN-001 remains implementing; REVIEW-001/DOC-001 stay pending. Direct external-tool support, practical final comparison/rename and local OS-actor limits, historical applicationSafetyPassed=false, All rights reserved and needs-review design remain intact.

A maintainer's concrete next check is the reviewed delivery commit pushed to `refs/heads/verify/native-readiness`, then observing the `Verify and release` run and its `verification-failure-<attempt>` artifact. README includes exact push/list/view/download instructions. The manual entry requires its documented default-branch availability; do not rely on it for an initially empty repository. These remote actions remain for a maintainer after review.

## Bounded interleaved-trace correction

Reclaimed as Automation maintainer; investigation/proposal committed before implementation. Original delivery `28c38b95f05f692b48637fc59dbb0b4de1fc9616` and its successful local evidence remain intact. Foundation `7c24ec8c2b8187679193ee571e8452f3930a13a9` is still an ancestor. Current integration `c086fe955c449426b06513840a3fd6fe6c36f3d0` has the same tracked tree as that foundation, so its intentional rollback was not merged over retained source. Origin is unchanged. No production, dependency, workflow, admission or threshold change is included.

The subsequent independent integration command failed after all nineteen browser cases (47.6 seconds), 93 assets and four lazy families. Its trace audit saw the unfinished portion of a scoped openat before its successful resumed fd. The retained result says failed, cleanup=true; the separate wrapper's success marker is invalid because a later cleanup command masked the failed AND-list. This history is not relabeled as success.

The audit now combines split records by thread identity and exact syscall before applying the existing access/extraction/extra-runtime restrictions. It rejects orphan, mismatched, duplicate, ambiguous and incomplete records, preserves negative returns, accepts the observed nested read-only device annotation and counts completed pairs. Returned descriptors still determine successful write destinations; AT_FDCWD cwd annotations do not authorize or reject an absolute scoped save.

Read-only replay succeeded against both full retained integration traces using their original executable, checkout, runtime directory and synthetic roots. No fixture was recreated. Supported: 392 unfinished/resumed pairs, 13,135 file-access calls, 9 scoped writes, one executable. Refused: 7 pairs, 381 accesses, 0 writes, one executable. Both have zero checkout accesses and no frontend extraction. SHA-256 before/after confirms unchanged trace bytes: supported `e83ccc6d1ea643a034960ff17e6e2d397e2b64953d53eda426273ae62ffbd6f1`; refused `d19ba12c78b6f17e1e55f24ff9466380030a137f47b678dabc34926b93372148`. Own evidence: `tmp/ci-rework-replay.json`, `.log`, `.exit` (0). Original failed result remains unchanged.

The first focused replay exposed an overly narrow new return validator for the actual `5</dev/urandom<char 1:9>>` annotation. Its exit 1 is retained separately in `tmp/ci-rework-replay-first.log` and `.exit`; a dedicated single/split device-read regression now passes. Initial focused lint formatting/backtracking findings were corrected. An initial test command without the explicit `./` path matched no files and is not test evidence. Final focused tmux command passed scoped ESLint, strict release types and `bun run test:release`: both strict projects, 18 tests / 0 failures / 97 assertions, including nine trace tests and real induced compile-failure/interruption cleanup. Evidence: `tmp/ci-rework-focused.log`, `.exit` (0).

The correction implementation is `74154938a579d996df00c0f0692290fdb3a8b2c7`. One complete mandatory command ran at that clean commit in the persistent project tmux shell: `bun install --frozen-lockfile && bun install --cwd web --frozen-lockfile && bun run check:ci && git diff --check`. The wrapper captures the AND-list status immediately, writes it before any independent cleanup, and exits with that same status. `tmp/ci-rework-full.exit` is 0; `tmp/ci-rework-full.log` retains the complete run. This correction did not repeat the broad command after a failure.

Both frozen installs passed unchanged. actionlint 1.7.12 validated the existing workflow and six unchanged action pins. File checks: 85/0 with 664 assertions and separate domain smoke; storage: 6/0 with 54 assertions; procfs: 1/0 with 4 assertions; backend: 122/0 with 1,172 assertions and 99.57% function / 99.44% line coverage; frontend: 43/0 across nine files and unchanged coverage (91.97% statements, 88.66% branches, 88.79% functions, 92.68% lines). Lint, strict types and production build passed. CI/release checks: 18/0 with 97 assertions. File counts overlap backend counts. The storage refusal diagnostic retained nativeAcceptance=pending, stages empty, expected CLI failure and cleanup=true; it is not native acceptance.

Actual runtimes remain Bun 1.4.2, separately executed Node v24.20.0, Linux arm64, project-local strace 6.13 and project-local Chromium. The fresh exclusive positive parent recorded in the retained correction evidence was observed overlayfs 0x794c7630/device 70; this checkout's tmp remained host-shared 0x6a656a63/device 41. The operator's project root was not relocated and write admission is unchanged.

The new `diagramdock-0.0.0-ci.fixture-linux-arm64` is 85,444,904 bytes, SHA-256 `f9f75ddc6fb1f17f63f7ea199addcfff5a8c631d177c13399a1bf726de85292f`. SHA256SUMS and independent hashing agree. Metadata, executable version/build-info, fixture tag and implementation commit agree. It embeds all 93 assets (91 JavaScript, one CSS, one HTML), whose actual HTTP bytes, hashes, MIME and security headers passed. There is no resource archive. Only the executable was copied outside checkout; PATH was empty and traces show one execve per service, no child runtime, checkout/frontend access or extraction.

All nineteen existing binary browser cases passed in 49.3 seconds, including authenticated browse/render/edit/save, independent Markdown byte preservation, external changes/conflicts/deletion, dirty/in-flight drafts and unsupported-root refusal. Flowchart, sequence, class and state lazy rendering passed, with zero unexpected errors or external requests. Supported trace: 342 paired calls, 13,423 accesses and 9 scoped writes. Refused trace: 5 pairs, 381 accesses and 0 writes. Evidence: `tmp/release-smoke-P2u0k9/result.json` (passed, cleanup=true), retained traces, inspected `lazy-diagrams.png`, and `tmp/e2e-cTHfXH`.

Actual loopback origins were `http://127.0.0.1:37455` (supported) and `http://127.0.0.1:44449` (refused). Both supervisors stopped with exit 0 and both URLs are now unreachable. Independent checks confirmed both roots, outside-checkout runtime, token, private configs and compile inputs absent, then removed only the empty owned positive parent. Evidence: `tmp/ci-rework-cleanup.json`, `.log`, `.exit` (0). Only the idle persistent shell remains.

Scoped self-review applied the shared and TypeScript backend policy to the parser, tests and audit caller. Exact identity/syscall association, retained negative returns, fail-closed incomplete evidence, unchanged write/source/runtime rules and regression discovery are verified. Remaining introduced findings: Critical 0, High 0, Medium 0, Low 0. Verdict: PASS for this bounded correction and local ARM64/overlay verification. Actual native/ext4, Linux x64 execution and remote CI/release remain pending; practical final-write-window and local OS-actor limits remain unchanged. No pushes, tags, releases, main integration, native probes or design changes occurred; prototype remains needs-review.


## Completion boundary

Completed the bounded implementation and local verification on 2026-09-07 22:05. Delivery acceptance remains partial because actual hosted/native/Linux x64 execution is pending. A final Linux x64 cross-build of the same implementation commit also passed compilation, 93-resource manifest/ELF/version/checksum assertions: 85,480,928 bytes, SHA-256 `5f924accb5c0cb8b78f6383ad0cc72e9bba1a84092c2a2f1039d8e7c8aeff64c`. Evidence: `tmp/x64-final.log`, `tmp/x64-final.exit` (0), `tmp/x64-cross-cDg3yh/`. It was not executed: this host is arm64 and has no installed x64 emulator. Cross-compilation does not satisfy the matching-runner gate. Both executable versions are nonpublishing fixtures, with no Git tags or remote release.
